# FT — Master Spec (living document)

> **What this is.** The single canonical record of how FT works *right now*. Updated after every shipped change. If something in production behaves differently from what's here, this doc is wrong and should be fixed.
>
> **What this is not.** Not a roadmap, not requirements. Future work lives in the deferred-specs list at the bottom; here only describes what's deployed.
>
> **Editing.** Click `Edit` to update inline. `Save` for a tweak; `Save as new version` for a substantive change (records the changelog).
>
> **Last meaningful overhaul:** 2026-06-01 (nineteenth pass) — **SC-17 Phase 1 shipped (v1.30.0): eToro statement importer — annual + YTD performance history.** Off `FT_Build_Handover_SC17_v3_supplement_2026-06-01.md` (Claude.ai). **First eToro-specific subsystem; namespaced `etoro_*` / `/api/etoro/*`** to stay clear of the unrelated Spec 9d `performance`/`/api/performance` trade-analytics namespace. **Propose-and-apply, Phase 1 never touches holdings** — it produces a read-only performance panel from uploaded eToro account statements (.xlsx). **Migration 0038** adds two denormalized tables: `etoro_performance_year` (per-year authoritative summary — range, is_ytd, realised/dividends/fees/interest/net + computed-pnl + recon-delta in USD & EUR, superseded/source_file/imported_at) and `etoro_performance` (per (year, asset_type) breakdown with **disc/copy column pairs** — realised_disc/copy_usd/eur — so the R1 strategy split survives without a null-bucket problem), each indexed `(user_id, year, superseded)`. New `internal/etoro` package: `Parse(r, fileName, now)` reads the xlsx via excelize (requires **Closed Positions** + **Financial Summary** sheets; Account Summary + Dividends optional), normalises asset types (Stocks/ETFs/Crypto/CFDs), and treats the **Financial Summary as the authoritative headline** — its P&L rows override the per-asset realised totals, net = sum of every FS numeric row, and a **reconciliation delta** warns when |EUR| > 1. **R1 two-axis split applied:** strategy bucket (discretionary vs copy) is driven by the Closed Positions `Copied From` column (`-`/empty = discretionary incl. discretionary CFDs; a username = copy); **`Type=CFD` is a wrapper/display flag only — never the strategy or asset-class signal.** Store layer: `UpsertEtoroPerformanceYear` (transactional supersede-on-reupload — marks prior live rows for that (user, year) superseded, inserts the fresh year + asset rows) + `ListEtoroPerformance` (live rows, newest year first, with asset breakdowns attached). Three endpoints (token-or-cookie via `requireUser`): `POST /api/etoro/import/preview` (multipart .xlsx → parsed + staged in an in-memory map keyed by userID, mutex + 10-min TTL, returns years + warnings), `POST /api/etoro/import/apply` (applies the staged statement), `GET /api/etoro/performance` (live annual + YTD). Frontend: a **Performance panel under the Summary tab** (`#etoro-perf-panel`) — empty-state with an Upload affordance, else an **All / Discretionary / Copy** strategy toggle (persisted to `localStorage` `ft.etoroStrategyView`), a YTD card, and per-year tables (realised / dividends / fees / net / trades + a Total row + recon flag), plus a two-step import modal (dropzone → preview → apply). Display currency follows the existing `display_currency` cookie / `state.summary.currency`. **Phase 2a (ISIN bootstrap) + Phase 2b (holdings reconciliation) explicitly deferred.** `go build`/`go vet ./...` clean; migration 0038 verified on a throwaway copy of the production DB (2 tables + 2 indexes; production untouched); `node --check web/app.js` + full `make build` clean. Production DB delta vs v1.29.0: `schema_migrations` gains 1 row (0038); 2 new tables created, both empty until first import. Remaining handover queue: SC-17 Phase 2a/2b (ISIN bootstrap + reconciliation).
>
> **Prior overhaul:** 2026-06-01 (eighteenth pass) — **Watchlist parity batch shipped (v1.29.0): SC-14 (SCORE → thesis tables) + SC-16 (manual-add enrichment) + SC-15 (grouped-by-sector view).** Off `FT_Build_Handover_SC14-16_Watchlist_Parity_2026-05-31.md`. **Same fix family as SC-10** — the Watchlist tab now matches the Stocks/Crypto tabs. **SC-14**: the Watchlist SCORE column was reading the empty legacy `framework_scores`/`watchlist_frameworks` path; re-pointed to `theses_index` (stocks) / `crypto_theses` (crypto) via the SC-10 overlay primitives — `Store.ThesisScoresByTicker` + `Store.CryptoThesisScoresBySymbol` keyed off the watchlist entries, resolved through `pickScore`/`pickCryptoScore` (thesis preferred, manual fallback) into a new `Score *scoreSummary` field on `watchlistRow`, rendered by the **shared `scoreCell()` helper** so rows show 🔒 lock / `/16` (stock) vs `/18 — <band>` (crypto) / stale marker / GitHub click-through identically to the holdings tabs; the Score column sort re-points to `e.score.totalScore`. **Schema surprise flagged (not corrected):** `theses_index` has no `band` column (band is crypto-only), so stock rows render `🔒 N/16 ✓` with no invented band. **SC-16**: manual ticker-add now enriches — `handleCreateWatchlist` calls a new best-effort `enrichWatchlistEntry` (8s timeout, non-destructive — only fills blank fields) that resolves company name + current price (`market.FetchStockQuote`: Finnhub→TwelveData→Yahoo) + free-text sector (`market.FetchYahooProfile`; crypto via `market.LookupCryptoBySymbol`). **Enrich-and-flag (D16.2): never guesses `sector_universe_id`** — there is no confident ticker→taxonomy mapping, so it is left NULL and the UI surfaces subtle `watchlistGapChips` — "needs data" (no price resolved) / "needs sector" (no taxonomy id) — that the user clears via Edit. **SC-15**: a flat ↔ by-sector **view toggle** (segmented control, choice persisted to `localStorage` key `ft.watchlistGroupBy`); grouped mode buckets rows by `sector_universe_id` into **collapsible sections** (`watchlistGroups` → caret + display name + count), ordered by `user_sector_ordering` (`display_order_user`) ascending when set else alphabetically, with the **"Unsectored" bucket always last**; within-group order preserves the active column sort; collapsed-group state is session-only; flat mode is byte-for-byte unchanged. Sector display names + order come from the existing `/api/sector-rotation/metrics` (`getSectorTagMap` extended to carry `order`). **No schema change / no migration.** Go change is confined to `internal/server/watchlist_handlers.go` (new `context`+`market` imports, the `Score` field, the per-entry overlay wiring, and the `enrichWatchlistEntry` helper); SC-14/SC-15 are frontend (`web/app.js` + `web/app.css`). `go build`/`go vet ./internal/server/...` clean on jarvis; `node --check web/app.js` clean; `make build` OK. Production DB delta vs v1.28.0: none (read-path + UI + best-effort enrichment only). Remaining handover queue: empty — SC-01→SC-16 + Spec 9p P1+P2 all shipped.
>
> **Earlier overhaul:** 2026-06-01 (seventeenth pass) — **Spec 9p P1+P2 shipped (v1.28.0): Macro Regime & Sector Rotation.** Off `FT_Build_Handover_SC01-10_Spec9p_2026-05-31.md` §154-223. **First backend feature since the SC frontend series** — new self-contained `internal/macroregime` Go package implementing a **deterministic growth × inflation quadrant classifier** by rate-of-change: **Q1 Goldilocks** (growth accel / inflation decel), **Q2 Reflation** (accel/accel), **Q3 Stagflation** (decel/accel), **Q4 Deflation/Recession** (decel/decel). **Reuses the Pal/9e FRED layer** (`cryptoindicators/providers.FREDClient`) — does NOT rebuild it. **Migration 0037** adds 5 tables — `macro_indicators` (series_id PK, value/prior/roc/direction/as_of), `macro_indicator_snapshots` (daily history for sparklines), `macro_regime_history` (append-on-change quadrant log w/ augmenting state + thematic flags + confidence + suggested-Jordi), `regime_playbook` (doctrine rows, stance CHECK favored|neutral|avoid), `ism_manual` (override log) — and seeds **38 §F doctrine rows** (Q1=8 / Q2=9 / Q3=11 / Q4=10). **13 FRED series**: 3 regional-Fed nowcasts (empire `GACDISA066MSFRBNY`, philly `GACDFSA066MSFRBPHIL`, dallas `BACTSAMFRBDAL`), `INDPRO`, `UNRATE` (inverted), `ICSA` claims (inverted), `CPIAUCSL`+`CPILFESL` (YoY computed from FRED history), `FEDFUNDS`, `WM2NS` M2 (13-week RoC), `T10Y2Y` curve (around-zero), `BAMLH0A0HYM2` HY-OAS credit, `DTWEXBGS` dollar. Classifier averages directional votes per axis (flat band 0.10), resolves the binary quadrant (ties→accel), and computes `augmentAndThematics` (rates/liquidity/curve/credit/dollar regime + flags liquidity_expansion/contraction, curve_inverted, credit_stress) + `confidenceOf` + `suggestJordi` (Q1/Q2→stable, Q3/Q4→defensive, stress modifiers tighten one notch). **ISM** via regional-Fed nowcast + **manual override** with a 35-day staleness gate. **6 endpoints** under `/api/macro/*`: regime read (`GET /api/macro/regime?historyDays=N` → indicators+sparkline + ISM + playbook for the live quadrant + regime + suggested-Jordi; `requireUserOrToken`), `POST /refresh` (manual FRED re-fetch + reclassify, 90s), `POST /ism` (set override 0-100, reclassifies immediately), `GET/POST/DELETE /playbook` (doctrine CRUD). **Nightly 01:00 UTC cron** (`RefreshAllAndSnapshot`, 5-min timeout) + a 45s warm-up `RefreshAll` on boot, both wired into `cmd/ft/main.go` via the existing `scheduleAt` pattern. **Frontend**: the Sector Rotation tab is **retitled "Macro Regime & Sector Rotation"** — a **collapsible macro band** renders on top (regime banner w/ quadrant tone + confidence, suggested-Jordi affordance, per-axis indicator cards w/ value + RoC + inline `macroSparkSvg` sparkline, augment chips, ISM override input, playbook **favoured/neutral/avoid** three-column doctrine) above the **existing 34-row sector table**; a **divergence overlay** (`divergenceMarker`) maps each sector row → playbook stance via fuzzy `SECTOR_PLAYBOOK_SYNONYMS` and renders ✓Favoured / ○Neutral / ✕Avoid inline, plus a ⚠ "holding a disfavoured sector" warning when an `avoid`-stance sector is held. **Suggest-a-Jordi-regime is suggest-only (never auto-set)** — the Apply button posts to the existing `POST /api/regime/jordi`; the authoritative regime stays a deliberate user action. **"Not investment advice" footing throughout.** **P3 explicitly deferred.** ACs G1-G8 met. `go build`/`go vet` clean; migration 0037 verified on a throwaway copy of the production DB (5 tables + 38 rows; production untouched); `node --check web/app.js` + full `make build` clean. Production DB delta vs v1.27.0: `schema_migrations` gains 1 row (0037); 5 new tables created + 38 playbook rows seeded; macro_indicators populate on first refresh. Remaining handover queue: empty — SC-01→SC-13 + Spec 9p P1+P2 all shipped.
>
> **Prior overhaul:** 2026-05-31 (night, sixteenth pass) — **Thesis authoring parity shipped (v1.27.0): SC-12 (crypto drop-box → parse → pre-fill D25) + SC-13 (stock guided form → strict MD → existing upload).** Off `FT_Build_Handover_SC12-13_2026-05-31.md` + the crypto format-spec supplement (Claude.ai). **Frontend-only — no Go/schema/DB change.** **SC-12** gives the Crypto Theses tab a drop zone (`#d25-ingest-zone`) that parses a crypto locked-thesis MD and pre-fills the **existing** D25 create form — NOT auto-create, NOT auto-lock (D12.1); the user reviews + locks through the normal path so cascades fire as today. `parseCryptoLockedThesis(text)` reads the full structure: title (em-dash regex), required `> **Final Score:** N/M`, Adapter line primary slug + `` `sub-type` `` **plus** the hybrid `+ <Adapter2> (… secondary)` round-trip (HYPE/XRP/HBAR), Holding Horizon, BTC-Beta, Parent Chain, per-pillar `### Q<n> … **score**` headers (cross-checked vs the `**Rounding:** … → **score.**` line), Q5 named mechanism → 14-value enum. **Parse-confident-or-blank-and-flag, never infer a score (D12.2).** Key insight: MD pillar sub-row counts do NOT match `D25_SCHEMA_BY_ADAPTER` (e.g. XRP l1 Q1 = 4 MD subs vs schema 3), so the parser trusts only the pillar **score** and synthesizes subCriteria as `new Array(def.subs.length).fill(score)` — all-equal deterministically rounds back via `d25ComputePillarScore`, so the **reconciliation badge** (`d25ReconRowHTML`, computed-vs-parsed total, red+delta on mismatch) shows green. Hybrid round-trip needed a one-line `d25BuildPayload` fix to emit `secondaryAdapterSlug` (backend already accepted it). Adapter-name surprise flagged: supplement writes `infrastructure`, D25 slug is `infra` (`CRYPTO_ADAPTER_NAME_TO_SLUG`). Acceptance test green offline — the six SC-11 theses (HYPE/XRP 11/18, OP 8/18, HBAR 10/18, ADA 8/18, SUI 7/18) parse GREEN; `XVM_v1_prefilter_block.md` is rejected by the title regex (a pre-filter rejection, not a thesis — must NOT enter `crypto_theses`). Existing manual D25 form unchanged (D12.3). **SC-13** gives the Stock Theses drop zone a **✍️ Guided form…** button (`#dz-guided`) → modal → `serializeStockThesisMD(d)` → the **existing** `POST /api/theses/upload` (no new table, no DB-first lifecycle — D13.1); live `#st-preview`, Patterns A/B/C, serializer guarantees em-dashes + `>`-prefixed metadata + standalone Final Score line; adapter validated against `STOCK_ADAPTERS` pre-upload (warn, don't fail — S-13a). Existing drop box unchanged (D13.2). Shared contract: SC-13 writes the format spec, SC-12 reads it. `node --check web/app.js` clean; offline parser + serializer harnesses green. Remaining handover queue: Spec 9p P1+P2 (macro regime panel + playbook/divergence overlay).
>
> **Prior overhaul:** 2026-05-31 (night, fifteenth pass) — **SC build series — SC-05 shipped (v1.26.0): Cowen weekly-capture autofill from live 9e indicators + suggest-only classification.** Fourth batch off `FT_Build_Handover_SC01-10_Spec9p_2026-05-31.md` (Claude.ai). Frontend-only (no Go/schema/DB change). The Cowen Sunday-capture modal gains a primary **⤓ Pre-fill from live indicators** button (the old "↻ Pre-fill from last submission" stays as a secondary ghost button). On click it reads `GET /api/crypto-indicators` (Spec 9e) and maps the latest readings into the form: `btc_vs_200wk_ma_pct` ← `cowen_price_vs_200wma` (stored as a ratio → converted to %), `btc_vs_200d_ma_pct` ← computed client-side from `GET /api/crypto-indicators/btc-history?days=205` (200-day SMA vs latest close — no dedicated indicator exists), `log_band_third` ← `cowen_log_band` score mapped from its 5-band scale to the form's 3 thirds (≥0.5 lower / ≤−0.5 upper / else middle), `risk_indicator` ← `cowen_risk_indicator` (0–1 passthrough), `btc_dominance_pct` + `btc_dominance_4wk` ← `cowen_btc_dominance` value + `trend4w` (±0.5% flat band → rising/flat/falling), `eth_btc` + `eth_btc_4wk` ← `cowen_eth_btc`. **Suggest-only (AC2): nothing is persisted until "Classify + save"** — the authoritative regime is always computed server-side by the existing `/api/regime/cowen/auto`. A live **regime-preview bar** (JS mirror of `regime.ClassifyCowen`, using the prior submission's cycle phase as `priorCyclePhase`) recomputes on every input/change event so the user sees STABLE/SHIFTING/DEFENSIVE/UNCLASSIFIED update as they edit; a suggested cycle phase is pre-selected from the risk indicator (>0.75→4, ≥0.5→3, ≥0.25→2, else 1) but stays fully editable (handover §96 — final cycle-phase + macro-flag call stays the user's). **MVRV-Z is always left blank** for manual entry (AC3 — no free data source). **Staleness gate (AC4):** any indicator whose `updatedAt` is older than a 10-day fallback window leaves its field blank rather than seeding a stale value; the suggestion note lists exactly which fields were filled vs left blank. **Schema-surprise flagged (not silently corrected):** the form's `log_band_third` is a 3-way (lower/middle/upper) while the 9e `cowen_log_band` indicator is a 5-band scale (lower/mid_lower/mid/mid_upper/upper) and exposes only a residual value + score (not the band string), so the prefill maps via score; and there is no 200d-MA indicator (computed from BTC history instead). `node --check web/app.js` clean. Remaining handover queue: Spec 9p P1+P2 (macro regime panel + playbook/divergence overlay).
>
> **Prior overhaul:** 2026-05-31 (night, fourteenth pass) — **SC build series — SC-09 + SC-06 shipped (v1.25.0): reusable contextual hover-explainers + regime-toggle tooltips.** Third batch off `FT_Build_Handover_SC01-10_Spec9p_2026-05-31.md` (Claude.ai). Frontend-only (no Go/schema/DB change). **SC-09** — a build-once/reuse-everywhere explainer: a single `FT_GLOSSARY` map (`term → {term, definition, calculation?}`) drives a shared floating popover (`ft-explainer-pop`) wired via **document-level event delegation** (`wireGlobalExplainers()`, called once at boot), so any `explainerMark(key)` ⓘ rendered now or by a future render Just Works with no per-render re-wiring (AC #3). `explainerMark` returns `''` for unknown keys, so value-derived keys can be dropped in unconditionally. Seeded six glossary terms (Dist to SL, Dist to TP, 12m Vol, Proposed SL, Score, Vol tier) + the three regime-toggle entries; markers attached to the Stocks-table headers (Proposed SL / Dist to SL / Dist to TP / 12m Vol / Score). **SC-06** — the Jordi quick-set modal's Stable/Shifting/Defensive buttons now carry the explainer ⓘ (first SC-09 consumer, AC #2). Copy reflects the live SC-08 two-tier proximity rule: Stable = entry-zone alerts fire + proximity at 75%; Shifting = proximity tightens to 85%, entry alerts still fire; Defensive = new-entry alerts silenced + proximity 85% + "effective regime = more defensive of Jordi and Cowen." `node --check web/app.js` clean. Remaining handover queue: SC-05 (Cowen autofill), Spec 9p P1+P2 (macro regime).
>
> **Prior overhaul:** 2026-05-31 (night, thirteenth pass) — **SC build series — SC-08 shipped (v1.24.0): dual stop-loss methodology + two-tier proximity alerting.** Second batch off the consolidated `FT_Build_Handover_SC01-10_Spec9p_2026-05-31.md` (Claude.ai). **Schema**: Migration 0036 adds `stock_holdings.sl_method TEXT NOT NULL DEFAULT 'vol_envelope' CHECK (sl_method IN ('technical','vol_envelope'))` + `sl_safety_pct REAL NOT NULL DEFAULT 0.02`; existing 24 rows backfill to vol_envelope/0.02. **Effective-stop resolution** (`metrics.EffectiveSL`): a manual `stop_loss` override always wins (source `"manual"`); otherwise the per-row method computes — `technical` = support1/support − volTier×ATR_weekly (tier multiples low 1.5 / medium 2.0 / high 2.5 / extreme 3.0); `vol_envelope` = avg_open × (1 − vol12m/100 − safety[default 0.02]). New `StockMetrics` fields `EffectiveSLPrice`/`EffectiveSLSource`/`DistanceToTPPct`/`ProgressToSL`/`ProgressToTP`; distance-to-SL + risk/reward now key off the effective stop. **Two-tier proximity family** (`internal/alert/proximity.go`, replacing the old flat-margin SL/TP proximity removed from `alert.go`): progress-based + symmetric across stop & target — `progressToSL=(entry−price)/(entry−SL)`, `progressToTP=(price−entry)/(TP−entry)`; AMBER at progress ≥0.75 (tightened to 0.85 when the effective regime is Shifting/Defensive), RED within 2% of the stop/target; RED supersedes AMBER per side; four distinct `notification_log` kinds (`proximity_{amber,red}_{sl,tp}`) so they dedup independently. Wired into `handleBotAlerts` with a regime-aware tighten flag → surfaces via the existing OpenClaw Telegram poll (no bot deploy). **UI**: Stocks table gains a **Dist to TP** column + a per-row **Tech/Vol** stop-method toggle under Proposed SL (`PUT /api/holdings/stocks/{id}/sl-method`, 🔒 hint when a manual stop overrides the method); Proposed SL now renders the resolved effective stop; Edit modal gains Stop-method + Vol-envelope-safety fields; header tooltips explain both methods. **Production DB delta vs v1.23.0**: `schema_migrations` gains 1 row (0036); `stock_holdings` rebuilt with 2 new columns (24 rows backfilled). `go build`/`go vet` clean; migration verified on a throwaway DB copy (production untouched); `make build` OK. Remaining handover queue: SC-09→SC-06 (hover-explainer + regime tooltip), SC-05 (Cowen autofill), Spec 9p P1+P2 (macro regime).
>
> **Prior overhaul:** 2026-05-31 (night, twelfth pass) — **SC build series Batch 1 shipped (v1.23.0) + full sector_adapter_subtype tagging catch-up row (v1.22.18)**. First batch off the consolidated `FT_Build_Handover_SC01-10_Spec9p_2026-05-31.md` (Claude.ai) — three orphaned-data fixes re-pointing UI at the now-populated tables. **SC-01**: the Summary "AI bottleneck chain" donut is now self-maintaining — derives slices at read-time from `stock_holdings.sector_adapter_subtype` via `buildBottleneckDonut` + `subtypeToBottleneckSlice` (replacing the hardcoded `buildStockTagDonut`); render gate relaxed to ≥1 tagged stock; legend carries per-slice ticker hover lists. Closed a plumbing gap — `sector_adapter_subtype` was migrated (0034/0035) but never surfaced in Go (`StockHolding.SectorAdapterSubtype` + `stockSelectCols`/`scanStock` now carry it). **SC-02**: Summary nudges re-pointed to real thesis tables — two collapsible panels (Coverage = holdings without a thesis; Freshness = >90d or `earnings_urgency='revision_needed'`); legacy `thesis_notes` nudge retired; `earnings_urgency` surfaced end-to-end. **SC-10**: crypto SCORE column now overlays locked `crypto_theses` (new `Store.CryptoThesisScoresBySymbol` joining on `coin_symbol`, latest locked per symbol; band rendered e.g. "13/18 — Accumulate") — fixes the bug where `handleListCrypto` called the stock-ticker lookup with crypto symbols (→ blank). Stocks unaffected (already overlaid `theses_index` since v1.19B). **v1.22.18** (snag S7 mechanical catch-up, folded into this commit): the production DB had been advanced to full tagging — all 24 `stock_holdings` rows carry a non-null `sector_adapter_subtype`, with CLS resolved to the new `ai-infra-semi:ems-odm` sub-type (closing the v1.22.17 deferred 7th re-tag) — but the changelog row was still pending; landed here so history stays linear. **Production DB delta vs v1.22.18 for the Batch-1 code**: none (read-path + UI only; no migration). FT rebuilt + redeployed; `go vet`/`go build` clean; `web/app.js` `node --check` OK; `/healthz` green. Remaining handover queue: SC-08 (SL/TP refinement), SC-09→SC-06 (hover-explainer + regime tooltip), SC-05 (Cowen autofill), Spec 9p P1+P2 (macro regime). Snags carried: S1 (custom-silicon-asic→Optical default), S2 (stale `theses_index.sub_type`).
>
> **Prior overhaul:** 2026-05-31 (early evening, eleventh pass) — **Stock holdings sector_adapter_subtype tagging shipped (v1.22.17)**. Per Claude.ai's three-answer response to my scope questions: (Q1) `sector_universe` retained (load-bearing for Spec 9f rotation tracker + sector_snapshots + heatmap + user_sector_ordering + sector_rotation_digests); (Q2) Option R — both taxonomies retained as orthogonal axes ("where it rotates" vs "how it's analyzed"); (Q3) Option B.ii — Migration 0034 acceptable; TEXT column not FK; single primary tag per Defense v1.1 §B5 dominant-segment-by-GP convention. **Migration 0034 SHIPPED**: `ALTER TABLE stock_holdings ADD COLUMN sector_adapter_subtype TEXT NULL` with CHECK constraint; new index `idx_stock_holdings_sector_adapter_subtype WHERE sector_adapter_subtype IS NOT NULL`. **Migration 0035 SHIPPED** (relax CHECK): initial 0034 used `LIKE '%-%:%'` which rejected single-word adapter slugs like `pharma:glp1-pure-play` (discovered when NVO re-tag failed); 0035 rebuilds the table with permissive `LIKE '%:%'` (just requires the colon separator); downstream code is canonical validator. Both migrations auto-applied on FT startup via embed runner; FT binary rebuilt twice + redeployed; `/healthz` green throughout. **6 of 7 stock_holdings re-tags applied**: ORCL → `cloud-infra:cloud-software-layer`; SU.PA → `industrial-electrical:diversified-ie`; GLD → `asset-hedge:gold-physical`; SLV → `asset-hedge:silver-physical`; MRVL → `ai-infra-semi:custom-silicon-asic`; NVO → `pharma:glp1-pure-play`. **CLS re-tag deferred pending Claude.ai sub-type decision**: thesis content explicitly flags "possible future EMS/ODM sub-sector split" — none of the 12 existing `ai-infra-semi` sub-types (gpu-accelerator / foundry / semicap-litho / semicap-etch-dep / chip-ip / memory-hbm / advanced-packaging / optical-datacom / cooling-thermal / specialty-chemicals / edge-silicon / quantum-frontier) capture CLS's identity as a hyperscaler-hardware ODM with CCS Connectivity & Cloud Solutions revenue mix 80% / HPS Hyperscaler Platform Services 42% / 1.6T CPO program / EMS positioning. Claude.ai's instinct was `advanced-packaging` but CoWoS/fan-out is TSMC/Amkor territory, not CLS's actual business. Closest existing fit is arguably `optical-datacom` (CCS 800G→1.6T is co-packaged optics) but the thesis itself anticipates a new `ems-odm` sub-type when watchlist accumulates Quanta/Foxconn/Wistron/Jabil. Three options sent to Claude.ai for CLS sub-type decision (see handover MD). **Production DB delta vs v1.22.16**: schema_migrations table gains 2 rows (0034 + 0035); stock_holdings table rebuilt with new column + index; 6 stock_holdings rows updated (ORCL/SU.PA/GLD/SLV/MRVL/NVO have non-null sector_adapter_subtype); CLS + 18 other holdings still NULL on the new column. **Sector_Holdings_Mapping v1.3 unblocked** for Claude.ai authoring — current holdings scope (25 entries); drops BE+MOD (watchlist-only in theses_index); adds CLS+MRVL+NVO; uses new `sector_adapter_subtype` column format. **Sprint window**: ~9-10 days remaining. Both Claude.ai and Claude Code on standby pending CLS sub-type decision + LINK v1 amendments + first user-authored D25 thesis + v0.5.1 audit (2026-08).

> **Prior overhaul:** 2026-05-31 (late afternoon, tenth pass) — **Round-trip follow-on closeouts (v1.22.16)**. Three small applications from Claude.ai's end-of-session deliverables landed after v1.22.15: (1) **ETH v1 Q2 prose hygiene amendment** per `Batch_3d_completion_record.md` §A — contradictory v0.4-style "Actually wait..." passage replaced with v0.5-correct prose at `/home/finbarr/Downloads/ETH_v1_locked.md` lines 48-54 + downstream reference at line 257 + new doctrinal note appended; verification: `grep "Actually wait"` returns 0 occurrences in body (only inside the new doctrinal note quoting it); DB state unchanged (Q2 = 2 was always stored). LINK v1 amendments still pending per Batch 3d spec §A1.1-A1.7 (user-applied locally). (2) **`Asset_Hedge_Adapter_v1.md` canonical replacement** — Claude.ai's 14,298-byte richer version (full schema sketch with industrial-demand `weight_by_subtype` matrix `{gold-physical: 0, silver-physical: 2, mixed-physical: 1, pgm-physical: 2}` + 4 sub-types `gold-physical`/`silver-physical`/`mixed-physical`/`pgm-physical` + rebalance triggers section + cross-adapter calibration section) replaces my interim 8,874-byte draft in both `sector_scorecards.markdown_current WHERE code='asset-hedge'` and `/home/finbarr/Downloads/`; interim file deleted; live DB updated atomically. (3) **Energy adapter `current_version` bumped 1 → 1.1** per `Energy_Sector_Adapter_v1_1_Lock_Supplement.md` (retroactive lock-resolution pattern — Energy was DB-locked but MD §7 unresolved; supplement formally closes 5 §7 decisions including SU.PA → Industrial-Electrical cross-resolution, XOM → Hydrocarbons, 6 sub-types retained, scoring math averaged-single-score for current implementation with vector storage deferred to Spec 9i, RR.L Energy-Power leg 11/16 confirmed); supplement §E "no DB UPDATE required (already locked)" — only `current_version` bumped. BTC adapter status flip note acknowledged — no action taken (trivial cosmetic; adapter already DB-locked since 2026-05-21 issue). README.md noted as a project-folder navigation aid; no DB action. **Production DB delta vs v1.22.15**: `energy-power.current_version` 1 → 1.1; `asset-hedge.markdown_current` 8,874 → 14,298 bytes; sector_scorecards row count unchanged (11/11 locked); crypto state unchanged (12 theses + 18 cascade rows + 4 history rows). **OPEN ITEM flagged back to Claude.ai — `stock_holdings` re-tag is BLOCKED on a schema-architecture decision**: the draft Sector_Holdings_Mapping v1.2 SQL `UPDATE stock_holdings SET sector_universe_id = '<adapter>:<subtype>'` won't apply as-written. Two architectural facts: (i) `stock_holdings.sector_universe_id` is `INTEGER NULL REFERENCES sector_universe(id)` — not a string column; (ii) the existing `sector_universe` table uses 34 legacy snake_case taxonomy codes (e.g., `gpus_ai_accel`, `precious_metals_gold`, `grid_transmission`) and has zero rows matching the new adapter framework codes. The new `sector_scorecards` table (11 rows post-v1.22.15) and the legacy `sector_universe` table (34 rows) are parallel taxonomies that aren't 1:1 mapped. Resolution requires one of: (A) add bridging rows to `sector_universe` matching new adapter framework codes, then UPDATE stock_holdings FKs; (B) Migration 0034 with new `sector_adapter_subtypes` table + 2nd FK from stock_holdings; (C) use existing `stock_holdings.sector` TEXT field as kebab-case `adapter:subtype` string (unstructured but no schema change). Three scope questions sent to Claude.ai to inform recommendation (see Claude_Code_to_Claude_ai_handover_2026-05-31_v1.22.16.md if authored). Sector_Holdings_Mapping v1.3 deferred until schema decision lands. **25-vs-24 holdings reconciliation captured**: actual `stock_holdings` is 25 unique tickers; mapping v1.2 documents 24 entries; net delta = mapping is missing **CLS** (Celestica, `ai_infra_semi` 11/16), **MRVL** (Marvell, `ai_infra_semi:custom-silicon-asic` 12/16), **NVO** (Novo Nordisk, `pharma:glp1-pure-play` 13/16); and contains **BE** (Bloom Energy, watchlist 13/16 in `theses_index`) + **MOD** (Modine Manufacturing, watchlist 11/16) which are NOT in stock_holdings. Per Claude.ai's response: v1.3 will be scoped to current holdings only (25 entries) when redrafted post-schema-decision. **`Master_Spec_Consolidation_Patch_2026-05-31.md`** structural section edits per Claude.ai's authored patch deferred — patch was authored against v1.22.10; v1.22.15 + this v1.22.16 row have overtaken most of its content. Recommended for future quiet pass; no operational impact. **Crypto-side feature group: D25 + D26 + D27 COMPLETE.** **Stock-side sector adapter sweep: COMPLETE.** **Sprint window**: ~9-10 days remaining; both Claude.ai and Claude Code on standby pending schema-architecture decision on holdings re-tag + LINK v1 amendments + first user-authored D25 thesis + v0.5.1 quarterly audit (2026-08). No urgent items in queue.

> **Prior overhaul:** 2026-05-31 (afternoon, ninth pass) — **Stock-side sector adapter framework structurally complete (v1.22.15)**. 5 remaining stuck sector adapters locked at v1.1 via Claude.ai-authored lock supplements (`Industrial_Electrical_Sector_Adapter_v1_1_Lock_Supplement.md` + `Cloud_Infra_Sector_Adapter_v1_1_Lock_Supplement.md` + `Mining_Metals_Sector_Adapter_v1_1_Lock_Supplement.md` + `Pharma_Sector_Adapter_v1_1_Lock_Supplement.md` + `Defense_Sector_Adapter_v1_1_Lock_Supplement.md`); each resolves 5-7 §7 open decisions per adapter. **New `asset-hedge` sector adapter spawned** (Mining-Metals §B Decision 7) — simplified 4-pillar /8 scorecard for physical metal ETFs (GLD, SLV, IAU, PSLV); distinct from Mining-Metals because storage products lack operational mining drivers; `Asset_Hedge_Sector_Adapter_v1.md` authored as standalone (~8.8KB). Cross-adapter calibration distribution validated coherent: 14/16 (RHM.DE, ASML), 13/16 (LLY, ABBV, ORCL), 12/16 (AEM, NVDA, SU.PA, RR.L defense leg), 11/16 (RR.L Energy-Power, XOM), 10/16 (ALB), 7/8 (GLD), 4/8 (SLV). Same v0.6.1 §D strict patch immutability discipline applied — original sector adapter MD bodies unchanged in `markdown_current`; lock supplements are standalone artifacts. **First time the immutability discipline has been applied outside the crypto framework** — cross-domain pattern locked. `sector_scorecards` inventory: **11 rows / 11 locked** (4 pre-existing + 6 newly-locked v1.1 + 1 newly-spawned v1.0 asset-hedge). Stock-side framework structurally complete. **This pass also folds in v1.22.11-v1.22.14 changelog rows** (D26 needs-review resolution UI / D25 Phase 2 polish per-adapter sub-criteria labels / D25 Phase 2 polish inline lock errors + BTC schema + M1-M6 fix / D27 fork-to-v2 workflow) which were previously batched for end-of-cycle bump per user direction. Production DB cumulative state: 12 crypto theses + 18 cascade rows + 4 history rows (LINK v0.5 re-lock pair + AAVE/BUIDL D26 acks); 11 sector_scorecards rows all locked; 39 stock theses all locked. **D25 + D26 + D27 = crypto feature group COMPLETE.** **Stock-side sector adapter sweep = COMPLETE.** Claude Code standby resumed pending: (1) Energy_Sector_Adapter v1.1 supplement for §7 Decision 1 (cross-adapter SU.PA classification resolution per Industrial-Electrical §G — small ~10min Claude.ai task), (2) Sector_Holdings_Mapping v1.2 (SU.PA re-tag + asset-hedge GLD/SLV mapping, small Claude.ai task), (3) first user-authored crypto thesis via D25 form (UX validation), (4) v0.5.1 quarterly audit cadence (2026-08).

> **Prior overhaul:** 2026-05-31 (morning, eighth pass) — **D26 needs-review resolution UI shipped (v1.22.11 + v0.9.3 supplement)**. New POST endpoint `/api/crypto/theses/{symbol}/{version}/acknowledge-cascade` transitions `needs-review` → `locked`, marks all unresolved `cascade_events` resolved, writes `event_rescore` history row with `event_reason='cascade_acknowledgment'` (LINK v0.5 §L.9.4 pattern reused; **no Migration 0034 needed**). New `internal/cryptotheses/cascade_acknowledge.go` with `AcknowledgeCascade(ctx, symbol, version, note) (*AcknowledgeResult, error)` service method. Errors: `ErrNotNeedsReview` (400 when caller tries to ack a `locked` or `draft` thesis), `ErrPPGNowFails` (400 when stored pillar scores no longer pass PPG — author must fork to v2 instead; defensive guard against data drift between flagging and ack). New `internal/server/crypto_theses_write_handlers.go::handleCryptoThesisAcknowledgeCascade` + `mapAcknowledgeErr`. Frontend: per-thesis detail modal now surfaces a `🔄 NEEDS REVIEW` banner (purple/indigo accent) with `Review cascade events + re-lock…` button when `status='needs-review'`. Clicking opens a confirmation modal listing all unresolved cascade events for the thesis (`dependency_type` + `priority` + `trigger_reason` + `action` + UTC timestamp) + optional acknowledgment note input + Acknowledge/Cancel buttons. POST roundtrip returns full `AcknowledgeResult` shape; success alert summarizes status transition + events resolved count + history row id; parent thesis detail modal + Crypto Theses tab re-render. **3 D26 ACs PASSED** via new `cmd/ft-d26-test/`: AC1 happy path (seeded thesis at `needs-review` with unresolved `cascade_events` row → AcknowledgeCascade → `status=locked` + event `resolved_at` populated + `resolution_note` matches caller-supplied + history row written); AC2 reject when `status=locked` (no-op forbidden) → `ErrNotNeedsReview`; AC3 reject when `status=draft` (not applicable) → `ErrNotNeedsReview`. **Production resolution applied**: AAVE + BUIDL (both held `needs-review` since Batch 3a + EIGEN cascade firing tests where their `cascade_events` rows were cleaned up during testing but status persisted) → acknowledged via new `cmd/ft-d26-resolve-prod/` one-off → both now `status=locked`, 2 `event_rescore` history rows with `event_reason='cascade_acknowledgment: D26 launch — historical needs-review from Batch 3a + EIGEN closeout cascade tests; pillar scores unchanged; re-locked for clean state going forward'` captured. Service handles "needs-review but no underlying cascade events" gracefully (`events resolved=0`). All 12 production theses now `locked`; zero `needs-review` lingering. New CSS: `d26-needsreview-box` (indigo accent + left border) + `d26-event-list` (mono-spaced cascade event readout). FT binary rebuilt via `make build`, deployed `/opt/ft/bin/ft`, service restarted clean, `/healthz` green, `curl http://127.0.0.1:8081/app.js` confirms 11 occurrences of `d26` + `acknowledge-cascade` in served bundle. **D25 + D26 = COMPLETE feature group.** `FT_Spec_9l_v0_9_3_supplement.md` (Claude Code as-built, D26) authored per v0.6.1 §D strict patch immutability — supplements v0.9 + v0.9.1 + v0.9.2 + v0.6.1; supersedes nothing. Production DB delta vs pre-D26: 2 history rows added (3 total now); status: 12 locked (was 10 locked + 2 needs-review); cascade_events still 0; cascade rows still 18; theses still 12.

> **Prior overhaul:** 2026-05-30 (evening, seventh pass) — D25 Scoring Engine COMPLETE (v1.22.10 + v0.9.2 supplement). Phase 3 end-to-end integration scenarios PASSED 6/6 via new `cmd/ft-d25-p3-test/`. Each scenario is multi-step chained (distinct from Phase 1 atomic ACs): (A) Standard DeFi creation — Create → Save → Re-edit → Re-save → Lock → verify cascade rows in DB; (B) DePIN mandatory RYR — Create with NULL RYR → Lock rejected → verify status remains draft (lock atomicity) → Populate RYR → re-lock succeeds; (C) RWA mandatory RABR + Custody — Create with `tier_3` custody → Lock succeeds (Tier 3 = soft warning per RWA adapter §3, NOT hard block) → verify persisted; (D) VETO triggered — Create all-2 pillars (would be 18/18 Strong) + `FounderRug=true` → Lock → band=Exit + `active_veto=founder_rug` (VETO overrides 18/18 Strong); (E) PPG cap — EIGEN-like Q8=0 → Lock → final = raw - 1 with `Q8_pillar_zero` in failed gates list; (F) End-to-end cascade firing — Lock DeFi+`OracleDependencyParentSymbol=LINK` → forward `oracle_dependency` row created → simulate LINK band drop Strong→Hold (2-band) → cascade engine fires 1 event flagging the new thesis → `status=needs-review`. **D25 ship-gate: COMPLETE.** Phase 1 + Phase 2 + Phase 3 all shipped in one session (~8 hours total vs 4-6 days estimated in build doctrine). Test infrastructure inventory: `cmd/ft-d25-p1-test/` (11 atomic ACs) + `cmd/ft-d25-p3-test/` (6 chained scenarios) + `internal/web/assets-test/d25_js_scoring_test.js` (15-fixture JS↔Go drift test) + `internal/cryptotheses/scoring_test.go` (14 Go unit fixtures) + 2 prior cascade firing harnesses = **53+ distinct test cases**. D26 (needs-review resolution UI per v0.6.1 §B) now unblocked — estimated 1 session. AAVE + BUIDL currently in `needs-review` state in production await D26. `FT_Spec_9l_v0_9_2_supplement.md` (Claude Code as-built, Phase 3) authored per v0.6.1 §D strict patch immutability. Backend Phase 1 + Phase 2 unchanged (no Go regressions; no JS regressions). Production DB unchanged (12 theses + 18 cascade rows; cascade_events still 0; history still 2 rows).

> **Prior overhaul:** 2026-05-30 (evening, sixth pass) — D25 Scoring Engine Phase 2 frontend shipped (v1.22.9). New `+ Create new thesis` + `📝 Draft theses…` buttons on Crypto Theses tab. New `state.cryptoView` dispatch (`list`/`drafts`/`create`/`edit:SYM:VER`). Three pages: drafts list (table with edit/delete), create form, edit-draft form. Form: 6-section layout — Identity / Pillar scoring grid with **inline rounding math annotation per Decision 3** (e.g., *"Sub-criteria [1,2,2,1] → avg 1.50 → exact tie, v0.5.1 #4 round DOWN → pillar = 1"*) / Q5 mechanism+quant (14-value enum dropdown) / adapter-specific (DePIN RYR or RWA RABR + Custody — conditional render per Decision 4) / Forward cascade declaration (DeFi+Speculative only, dropdown of locked Infrastructure theses per Decision 5) / VETO checklist (universal 6 + adapter-specific) / live computed summary panel (sticky right side — raw total/raw band/PPG/VETO/final band; VETO override banner when triggered) / action buttons (save/lock/delete/cancel). JS port `d25ComputePillarScore` mirrors Go `ComputePillarScore` byte-identically with **v0.5.1 #4 tie-break LOCKED** per v0.6.1 §A — drift-prevention validated via Node test at `internal/web/assets-test/d25_js_scoring_test.js` — **15/15 fixtures PASS** (10 pillar fixtures + 5 band fixtures). Sub-criteria labels schema (`D25_SCHEMA` in app.js) hard-coded per architectural Q A option B — labels for 9 alt_18 pillars + 6 monetary_12 pillars. Phase 3 will extract from adapter MDs. New CSS section (`d25-*` prefix) for form layout, sticky summary pane, segmented sub-criterion controls, inline math display, VETO banner, custody tier_3 soft warning. UI sequencing: form save creates draft → redirects to drafts list; form lock saves THEN calls /lock endpoint THEN redirects to thesis list with summary alert. Backend Phase 1 unchanged (10/10 ACs from v1.22.8 still pass). Phase 3 end-to-end integration scenarios remain as final test pass. `FT_Spec_9l_v0_9_1_supplement.md` (Claude Code as-built, Phase 2) authored per v0.6.1 §D strict patch immutability.

> **Prior overhaul:** 2026-05-30 (evening, fifth pass) — **D25 Scoring Engine Phase 1 backend shipped (v1.22.8)** per `D25_Build_Doctrine_Handoff.md` 6 scope decisions + `FT_Spec_9l_v0_6_1_supplement.md` doctrinal resolutions. 5 new endpoints: `POST /api/crypto/theses` (create draft), `PUT /{symbol}/{version}` (update draft, 400 on locked/needs-review per v0.6.1 §B), `POST /{symbol}/{version}/lock` (transition + cascade auto-create + VETO override + PPG cap), `GET /drafts`, `DELETE /{symbol}/{version}`. New `internal/cryptotheses/scoring.go` with `ComputePillarScore` (v0.5 strict + **v0.5.1 #4 tie-break LOCKED per v0.6.1 §A** — round DOWN on exact 0.5/1.5 ties, retroactively applied to AAVE Q1 + EIGEN Q1 fixtures) + `ComputeRawAndFinalBand` (PPG cap single-band-below per Spec 9l §15, never stacked). New `internal/cryptotheses/thesis_write_service.go` with `DraftThesisInput` shape, `ValidateForLock` (DePIN RYR + RWA RABR + Custody Tier mandatory enforcement per Decision 4 — hard, no soft-warn override; pre-revenue DePIN edge case accepts `q5_paid_revenue_usd=0` not NULL), `VetoConditions` struct (universal 6 + adapter-specific map; `ApplyVeto` forces band=Exit overriding pillar math + PPG per Spec 9l §20), Lock transaction with auto-cascade-creation (upward `protocol_host` from `primary_chain_symbol` + `btc_beta_implicit` from `btc_beta` field skipping `reference` self-cascade + forward `oracle_dependency` from Decision 5 structured field with parent-must-be-Infrastructure validation + self-reference rejection + duplicate-row rejection) + `initial_lock` history row. Compound `pillar_scores_json` shape `{"Q1":{"subs":[...],"score":N},...}` per Decision 1 ("store both"); legacy `{"Q1":N}` preserved for 12 seed fixtures via dual-mode `ParsePillarScoresJSON` reader. Transient lock-time fields (primary chain, oracle parent, VETO conditions) persisted as `__meta__:` prefix tags in `secondary_tags_json` — avoids Migration 0034 escalate per v0.6.1 §D; `FilterMetaTags` strips them from public read paths. Legacy `ApplyV05Rounding` helper now wired to `ComputePillarScore` for byte-identical behavior. `cryptoWrite *ThesisWriteService` added to Server struct + initialized after ctor (depends on `cryptoAdapters` + `cryptoCascade`). New routes wired in `server.go`. **11 P1 ACs PASSED** via new `cmd/ft-d25-p1-test/` ephemeral SQLite harness: AC1 (POST DeFi → Q1=1 tie-break verified), AC2 (DePIN lock missing RYR → 400), AC3 (RWA lock missing custody → 400), AC5 (invalid Q5 enum → 400), AC6 (PUT draft → 200), AC7 (PUT locked → 400), AC8 (lock creates upward cascade rows), AC9 (lock with oracle_dependency parent creates forward row + DB row count = 1), AC10 (VETO → band=Exit + veto_triggered=true), AC11 (AAVE/EIGEN/5+pillar fixtures), AC12 (PPG cap one-below, gates list includes `Q8_pillar_zero`). AC4 (sub-type validation) deferred — sub-type currently free-text on adapter MDs; Phase 2 will surface dropdown sourced from adapter schema. Production DB unchanged (12 theses + 18 cascade rows). Status carve-out for `needs-review` confirmed working (AAVE/BUIDL would return 400 on PUT/lock — D26 follow-on backlog item per v0.6.1 §B). FT binary deployed to `/opt/ft/bin/ft`, service restarted clean. **Phase 1 ship-gate moved 8/10 → 10/10** (the 2 deferred validators landed here). **Phase 2 (frontend dedicated pages) pending.** Claude Code standby pending Phase 2 sequencing call from user. `FT_Spec_9l_v0_9_patch.md` (Claude Code as-built, Phase 1) authored separately per v0.6.1 §D strict patch immutability.

> **Prior overhaul:** 2026-05-30 (PM late evening, fourth pass) — EIGEN v1 ingest + v0.8 patch. 12th locked thesis lands as second Infrastructure seed (first non-oracle sub-type — restaking) at 9/18 raw → Trim via PPG cap. ETH→EIGEN protocol_host moderate + BTC→EIGEN btc_beta_implicit weak cascade rows added → **12 theses + 18 cascade rows** live. **First multi-zero pillar thesis** (Q2/Q5/Q8 all 0; single one-band-below cap, not stacked — verified). **First aspirational mechanism thesis** (Q5 `governance_with_fee_switch` per ELIP-12 proposed, not active — $0 current project revenue). **Lazy-forward-cascade pattern validated** (zero forward `oracle_dependency` rows pre-created at lock — Infrastructure adapter §3 doctrinal commitment). 5 cascade firing acceptance tests 5.3-5.7 all PASSED via new `cmd/ft-cascade-firing-eigen`. Infrastructure adapter intra-adapter range now 7 points (LINK 16 → EIGEN 9) — widest in framework, validates adapter discrimination. 5th v0.5.1 doctrinal candidate queued for 2026-08 quarterly audit (aspirational vs operational mechanism scoring codification). Phase 1 ship-gate unchanged at 8/10. Claude Code standby posture resumed.

---

## 1. What FT is

A single-user portfolio dashboard for Fin Curran. Tracks stock + crypto holdings, regime overlay, sector rotation, performance retrospective, transaction history, framework scoring, and a thesis observation log. Deployed at `https://ft.curranhouse.dev` (Cloudflare Tunnel → `127.0.0.1:8081`).

Built in Go + SQLite + vanilla HTML/JS, no framework, no bundler. Runs in ~80 MB RSS on `jarvis` alongside HCT.

## 2. Layout — tabs in order

| # | Tab | Spec | Role |
|---|------|------|------|
| 1 | Summary | 2 | KPIs, donuts (asset / core-alt / sector / bottleneck / phase), stale-score banner, stale-thesis banner, regime pills, market pill |
| 2 | Stocks & ETFs | 3, 9c, 12 | Holdings table with alerts, proposed SL/TP (price\|%), 12m vol, score, sector-flow pill |
| 3 | Crypto | 3, 9c, 12 | Crypto holdings with classification (auto Core/Alt), current location, 12m vol, score |
| 4 | Performance | 9d | Closed-trades retrospective: window pill, R-multiple histogram, equity curve, methodology calibration, cohort drill-down |
| 5 | Screener | 9b | S&P sample with filters; "+ watchlist" prefilled |
| 6 | Sector Rotation | 9f | 34-row taxonomy (17 AI + 6 non-AI + 11 GICS), multi-window returns, RS vs SPY, tag pills, drag-reorder, weekly digest |
| 7 | Scorecards | 9g | Adapter MD repository (Philosophy + Energy + Hydrocarbons + Master Spec). Two-pane viewer/editor |
| — | Crypto Indicators | 9e | BTC-primary regime layer — Cowen 4-phase, Pal macro, ETF flows, F&G, stablecoin supply |
| — | Crypto Theses | 9l | 8-adapter Repository (BTC + L1 + L2 + DeFi + Infra + DePIN + RWA + Speculative). Phase 1 = adapter repository only; Scoring Engine + per-coin theses pending locked adapter MDs |
| — | Signals | 9k | Political + insider signal tab (SEC EDGAR per-ticker, capitol-trades, OGE) |
| — | Stock Theses (was "Theses") | 15 | Renamed 2026-05-29 to disambiguate from new Crypto Theses sibling. Same GitHub-backed library + earnings-revision warnings |
| 8 | Watchlist | 4, 9b, 12 | Names being considered, framework-scored, analyst Bear/Base/Bull, sortable, promotable |
| 9 | Heatmap | 6, 9d | SVG treemap. Three modes: market_cap / my_holdings / pnl |
| 10 | News | 2 D6, 12 D10 | NewsAPI feed + stocks F&G chip + macro calendar cards + filter mode |
| 11 | Crypto News | 2 D6 | CryptoPanic feed + alternative.me F&G chip |
| 12 | Settings | 3, 9b, 9c, 9c.1, 7, 11 | Portfolio risk dashboard, LLM spend dashboard, diagnostics + provider health, deleted-holdings restore, audit log, regime history, **Spec docs (this section)** |

Top bar: brand · market pill (clickable for all 7 exchanges, click-to-focus) · regime pills (Jordi / Cowen / Effective) · refresh status · refresh / import / export / ⌘K palette / logout.

## 3. Schema — every migration

| # | File | Adds |
|---|------|------|
| 0001 | `init.sql` | `users`, `sessions`, `service_tokens`, `stock_holdings`, `crypto_holdings`, `meta`, `news_cache`, `notification_log` |
| 0002 | `daily_change` | `daily_change_pct` on stock/crypto |
| 0003 | `crypto_is_core` | `is_core` on crypto (BTC/ETH=1) |
| 0004 | `spec3_holdings_extensions` | `note`, `deleted_at`, `beta`, `earnings_date`, `ex_dividend_date`, `vol_tier`. `holdings_audit`, `price_history` tables |
| 0005 | `watchlist_frameworks` | `watchlist`, `framework_scores` (append-only) |
| 0006 | `exchange_override` | `stock_holdings.exchange_override` |
| 0007 | `user_preferences` | k/v store, seeded `heatmap_mode=market_cap` |
| 0008 | `regime_history` | Append-only regime log; preference seeds for current state |
| 0009 | `percoco_execution` | Spec 9c columns: `support_1/2`, `resistance_1/2`, `atr_weekly`, `vol_tier_auto`, `setup_type`, `stage`, `tp1/tp2_hit_at`, `time_stop_review_at`. New: `portfolio_value_history`, `sr_candidates`, `daily_bars`, `weekly_bars`. 8 risk-cap preference seeds |
| 0010 | `llm_cost_discipline` | `llm_usage_log`, `llm_usage_daily`. 25 user_preferences seeds (caps, model, tools=0, kill-switches) |
| 0011 | `performance` | `closed_trades` (UNIQUE source_audit_close_id), `performance_snapshots` |
| 0012 | `transactions` | `transactions` (append-only, supersede column), `dividends`. `thesis_link` + `realized_pnl_usd` on holdings. Backfill: synthetic `opening_position` per active holding |
| 0013 | `thesis_notes` | Append-only observation log (target_kind, factor_id, factor_direction, source_kind) |
| 0014 | `provider_health` | One row per external provider tracking last_success/failure + counts |
| 0015 | `spec12_batch_a` | Cash balance preference seeds + focused_exchange seed + `holdings_audit.reason_code` |
| 0016 | `spec12_batch_b` | `crypto_holdings.current_location`, `volatility_12m_pct` on both holdings tables |
| 0017 | `spec12_batch_c` | Forecast columns (low/mean/high/fetched_at) on watchlist + stock_holdings |
| 0018 | `spec12_currency` | `stock_holdings.currency` (autofilled from Yahoo) |
| 0019 | `sector_rotation` | `sector_universe` (34 rows), `sector_snapshots`, `user_sector_ordering`, `sector_rotation_digests`. `sector_universe_id` on holdings + watchlist. 22/24 holdings backfilled |
| 0020 | `scorecards` | `sector_scorecards`, `sector_scorecard_versions`. Seeded 5 docs (Philosophy + Energy-Power + Hydrocarbons + Pharma + Master Spec) |
| 0021 | `holding_theses` | `holding_theses` (one per kind+holding_id), `holding_thesis_versions` (append-only history) |
| 0022 | `mapping_v1_1_retag` | Refresh-session re-tag of ORCL → `data_center_reits` and WPM → `precious_metals_gold` per Sector_Holdings_Mapping_v1.1 |
| 0023 | `theses_index` | Spec 15 — GitHub-backed thesis library cache |
| 0024 | `crypto_indicators` | Spec 9e Phase 1 — `crypto_indicators`, `crypto_indicator_snapshots`, `crypto_indicator_weights`, `crypto_composite_snapshots` |
| 0025 | `btc_price_history` | Spec 9e — daily BTC OHLC for Cowen log-band + 200WMA |
| 0026 | `pal_ism_to_cfnai` | Spec 9e — swap ISM proxy to CFNAI series |
| 0027 | `signals` | Spec 9k Phase A — political + insider event store |
| 0028 | `signals_issuer` | Spec 9k — issuer-side attribution columns |
| 0029 | `signals_committee_seed` | Spec 9k — committee allow-list seed |
| 0030 | `signal_events_oge` | Spec 9k Phase B — adds 'oge' to signal_type CHECK + 'HOLD' action |
| 0031 | `crypto_theses` | Spec 9l Phase 1 — `crypto_adapters` + `crypto_adapter_versions` + `crypto_theses` + `crypto_thesis_history` + `crypto_thesis_dependencies` + `cascade_events` + `crypto_allocation_current` + `crypto_allocation_history`. 8 adapters seeded as drafts |
| 0032 | `crypto_theses_speculative_horizon` | Spec 9l v0.3 — BEFORE INSERT/UPDATE trigger pair on `crypto_theses`. Blocks Speculative adapter theses from locking at Never-Sell/Cycle/Multi-year horizon. Trade/Medium/TBD allowed |
| 0033 | `crypto_theses_q5_ryr_rabr_custody_btc_ref` | Spec 9l v0.6 doctrinal + v0.7 as-built. **Five doctrinal items in single migration.** Item 1: Q5 mechanism CHECK 7→14 values (added `direct_asset_claim`, `required_for_service`, `dsr_surplus`, `burn_and_mint`, `buyback_stake`, `real_yield_staking`, `governance_with_fee_switch`). Item 2: DePIN RYR cross-pillar columns (`q4_q5_ryr`, `q5_paid_revenue_usd`, `q5_emissions_usd`, `network_age_months`). Item 3: RWA RABR cross-pillar columns (`q5_rabr`, `q5_verified_asset_value_usd`, `q5_token_supply_at_par_usd`, `q5_audit_date`, `q5_auditor`). Item 4: RWA Custody Verification Tier (`q6_custody_tier` with CHECK + `q6_custody_cadence` + `q6_custody_jurisdiction`). Item 5: BTC β CHECK 4→5 values (added `reference`). Implemented as table rebuild (SQLite CHECK replacement requires it; all column adds folded into same rebuild for clean schema). 8 indexes + 2 Speculative horizon triggers from 0032 re-created. Post-migration UPDATEs: 5 Q5 re-tags (LINK→required_for_service, AAVE→real_yield_staking, BUIDL→direct_asset_claim, LUNC→burn_and_mint, RNDR→burn_and_mint) + BTC β `low`→`reference` + RNDR RYR populate (0.35, $2.7M, $8M, 30 months) + BUIDL RABR populate (1.00, $2.45B, $2.45B, 2026-04-30, BNY Mellon) + BUIDL Custody populate (tier_1, monthly, United States). |

## 4. Background jobs

| Job | Schedule | What |
|-----|----------|------|
| Live refresh | `FT_REFRESH_INTERVAL` (default 15m) | FX → stocks → crypto → heatmap. Provider chain: Finnhub → TwelveData → Yahoo |
| Daily job | 04:00 UTC | 365-day price_history backfill, calendar dates, beta auto-resolve, 12m vol compute, analyst forecasts. CLI: `ft daily` |
| Sector rotation ingest | 22:00 UTC | 34-ETF + SPY + VWRL daily close. CLI: `ft sector-ingest` |
| Weekly sector digest | Fri 22:00 UTC | Top/bottom 5 by RS, WoW movers, newly-tagged "rotating in" → `sector_rotation_digests` |
| Sunday regime nudge | Sun 18:00 UTC | FT bot Telegram nudge unless `regime_skip_week` set or Cowen submitted in last 7 days |
| Weekly perf summary | Sun 19:00 UTC | FT bot — 30-day perf snapshot |
| Session GC | Hourly | Purges expired session rows |
| DB backup | 03:15 UTC (cron) | `sqlite3 .backup` to `/var/backups/ft/`, prunes >14d |

## 5. Endpoints — by spec

(Cookie auth unless marked **T** for cookie-or-token.)

**Auth:** `GET /api/auth/{state,me}`, `POST /api/auth/{setup,login,logout}`

**Holdings:** `GET /api/holdings/{stocks,crypto}`, `POST` / `PUT/{id}` / `DELETE/{id}` / `/{id}/restore` / `/deleted`. `PUT /api/holdings/stocks/{id}/sector` (9f).

**Summary + status:** `GET /api/summary`, `GET /api/marketstatus`, `GET /api/marketstatus/all`, `GET /api/audit`

**Refresh:** `POST /api/refresh` **T**, `GET /api/refresh-status` **T**

**Import/export:** `POST /api/import/{preview,apply}`, `GET /api/export.xlsx`, `GET /api/export.csv?tab=stocks|crypto|watchlist` (v1.5)

**Spec 15 — Thesis Library:** `GET /api/theses{,/gaps,/{id}}`, `POST /api/theses/{upload,scoring-log,sync}` (v1.6, scoring-log added v1.7). GitHub repo `finbarrcurran/cross_sector_research` is the source of truth; FT keeps a local clone at `/var/lib/ft/research/` synced every 5 min. Theses rows tagged with ownership (owned/watchlist/other) via JOIN to stock_holdings + watchlist; UI groups them into three sections (v1.7).

**Heatmap:** `GET /api/heatmap.svg?mode={market_cap|my_holdings|pnl}&sector=`

**News + F&G:** `GET /api/news/{market,crypto}`, `GET /api/feargreed{,/stocks}`

**Spec 4 — Watchlist + Frameworks:** `GET/POST /api/watchlist`, `PUT/DELETE/{id}`, `POST/{id}/promote`. `GET /api/frameworks{,/{id}}`. `GET/POST /api/scores`

**Spec 6 — Preferences:** `GET/PUT /api/preferences/{key}` **T**

**Spec 9b — Regime:** `GET /api/regime` **T**, `POST /api/regime/{jordi,cowen/manual,cowen/auto}`, `GET /api/regime/history`, `GET /api/screener`, `GET /api/macro`

**Spec 9c — Percoco:** `GET /api/holdings/{stocks,crypto}/{id}/levels` **T**, `POST .../autoscore`, `GET /api/risk/dashboard` **T**, `POST /api/risk/snapshot` **T**

**Spec 9c.1 — LLM:** `GET /api/llm/{spend,log}` **T**, `POST /api/llm/{pause,override,override/clear}` **T**

**Spec 9d — Performance:** `GET /api/performance/{overview,cohorts,calibration,cohort/{key},export.csv}` **T**

**Spec 10 — Transactions:** `GET/POST /api/transactions`, `POST /api/transactions/{id}/supersede`, `GET /api/holdings/{kind}/{id}/taxlots`, `GET/POST /api/dividends`, `POST /api/transactions/import`

**Spec 11 — Thesis notes:** `GET/POST /api/notes` **T**, `PUT/DELETE /api/notes/{id}` **T**, `GET /api/notes/{stale,contradictions,resolve}` **T**

**Spec 7 — Diagnostics:** `GET /api/diagnostics` **T**

**Spec 12 — Lookup:** `GET /api/lookup/ticker?q=&kind=`

**Spec 9f — Sector rotation:** `GET /api/sector-rotation/{metrics,sectors,digests}` **T**, `POST/DELETE /api/sector-rotation/ordering`, `POST /api/sector-rotation/refresh` **T**

**Spec 9g — Scorecards:** `GET /api/scorecards{,/{code}{,/versions}}` **T**, `PUT /api/scorecards/{code}`, `POST /api/scorecards/preview`, `PUT /api/scorecards/{code}/status`

**Spec 14 — Per-holding theses:** `GET/PUT /api/holdings/{kind}/{id}/thesis`, `GET /api/holdings/{kind}/{id}/thesis/versions`, `PUT /api/holdings/{kind}/{id}/thesis/status`, `POST /api/holdings/{kind}/{id}/thesis/preview`

**Bot:** `GET /api/bot/{alerts,holdings/summary,holdings/movers}` **T**, `POST /api/bot/alerts/ack` **T**, `POST /api/bot/refresh` **T**

## 6. FT Telegram bot

Standalone Node 22 daemon at `/opt/ft-bot/`, system user `ft-bot`. Bearer-token auth. Bot identity `@FinsFTAlerts_bot`.

**Reactive commands:** `/alerts /summary /movers /regime /skip /levels /size /risk /positions /done /llm /perf /note /snooze /unsnooze /help`

**Proactive crons:**
- 13:00 / 17:00 / 21:00 UTC weekdays — RED/AMBER alerts with `notification_log` dedup
- Sunday 18:00 UTC — regime nudge
- Sunday 19:00 UTC — weekly perf summary

`/snooze [hours]` writes `alerts_snooze_until` preference; proactive crons short-circuit when set.

## 7. Conventions

- Schema changes → new migration `internal/store/migrations/00NN_foo.sql`
- All numeric columns get `class="num"`
- Frontend is vanilla JS in `web/app.js`; no framework, no bundler
- Cache-busting via 8-char hash of `app.js+app.css` stamped into index.html as `?v=`
- Every mutation writes a `holdings_audit` row with `changes_json` + optional `reason_code`
- `user_preferences` is the home for any new k/v setting; per-key validation in `validPreferenceValue()`
- All endpoints accept cookie auth + `ft_st_…` bearer token where the bot needs them
- Append-only tables (`transactions`, `closed_trades`, `thesis_notes`, `sector_snapshots`, `sector_scorecard_versions`, `framework_scores`, `holdings_audit`) — corrections via supersede/soft-delete, never UPDATE on data columns
- **Master Spec is bumped on every completed section or spec** (set 2026-05-19, refined same day). A "section" = a spec shipping, a feature batch completing, a sub-phase landing, a polish batch wrapping, or an architectural decision being made. **Not** every commit — typo fixes, single parser tweaks, comment-only changes, test-only commits do NOT bump on their own; they roll up into whatever section they're part of. Versioning: patch (v1.7.x) for polish batches, minor (v1.8) for new specs/features, major (v2.0) for breaking architecture. Each bump: (a) new row in §13, (b) commit includes the spec change, (c) live DB updated via SQL UPDATE after deploy.

## 8. Provider chain

| Domain | Primary | Fallback | Free-tier notes |
|--------|---------|----------|-----------------|
| US stock quote | Finnhub | TwelveData → Yahoo | TwelveData free is US-only since 2024 |
| Non-US stock quote | Yahoo (crumb dance) | — | Fragile; updates every few months |
| Stock history (sparkline + 12m vol) | Yahoo `v8/chart` | — | 365-day window |
| Stock fundamentals (beta, calendar, targets) | Yahoo `quoteSummary` | — | Patchy for non-US |
| Crypto quote | CoinGecko `/simple/price` | — | Bursts trigger sticky 429 |
| Crypto history | CoinGecko `/market_chart` | — | Sequential w/ 2.5s gap |
| FX EUR→USD | Frankfurter | static fallback 1.08 | No key needed |
| Stocks F&G | CNN `dataviz.cnn.io` | — | Unofficial; UA-gated |
| Crypto F&G | alternative.me | — | Stable |
| News (stocks) | NewsAPI | stale cache | Free 100 req/24h |
| News (crypto) | CryptoPanic | stale cache | Free tier sufficient |
| Sector ETFs | Yahoo daily closes | — | 36 ETFs × 1 call/day |

All provider calls wrapped with `health.Record` (Spec 7) for diagnostics surfacing.

## 9. Environment variables

In `/etc/ft/env` (mode 0600, root:root). See `deploy/env.example` for the full list with signup URLs.

Required: `FT_FINNHUB_API_KEY` (else US quotes fail).

Optional: `FT_TWELVEDATA_API_KEY`, `NEWSAPI_API_KEY`, `CRYPTOPANIC_API_KEY`, `FT_ANTHROPIC_API_KEY`, `FT_TELEGRAM_BOT_TOKEN`, `FT_TELEGRAM_CHAT_ID`, `FT_GITHUB_TOKEN` (Spec 15 Thesis Library, fine-grained PAT scoped to cross_sector_research).

Runtime: `FT_ADDR`, `FT_DB_PATH`, `FT_REFRESH_INTERVAL`, `FT_COOKIE_SECURE`.

Off-site backup creds (NOT in `/etc/ft/env` — they live in rclone's own config): `/var/lib/ft/.config/rclone/rclone.conf` holds the Cloudflare R2 access key + secret + endpoint for daily DB snapshots (v1.7.3). Mode 0600, ft:ft. Bucket: `ft-backups`. 90-day retention.

## 10. Iteration loop

1. Edit `staging/ft/` on laptop
2. `git add . && git commit -m "..." && git push`
3. On jarvis: `sudo /opt/ft/bin/deploy.sh` — pulls, builds, installs, restarts, healthz-checks
4. Update this Master Spec to reflect any behavioural change

## 11. CLI subcommands

```
ft                                       run server
ft serve                                 run server (explicit)
ft seed [--user-id N]                    load Fin's holdings
ft daily [--user-id N] [--days 365]      Spec 3 D8/D10/D11 daily job + Spec 12 12m vol
ft backfill-bars [--user-id N] [--range 2y]  Spec 9c daily OHLC
ft perf-derive [--user-id N]             Spec 9d derive closed_trades + snapshots
ft sector-backfill [--months 14]         Spec 9f one-off ETF history
ft sector-ingest                         Spec 9f manual daily ingest
ft token create --user-id N --name X     mint a bearer token (plaintext shown once)
ft token list                            list tokens (no plaintext)
```

## 12. What's deferred — known queue

| Spec | Purpose | Status |
|------|---------|--------|
| 9e | Correlation matrix tracking | Drafted in v2; awaiting 30+ positions to justify |
| 9h | Real-time technicals monitoring (Finnhub WebSocket level breaks) | Drafted in handoff; defer until alert noise is a felt problem |
| 9i | Adapter scoring engine | Drafted in handoff; depends on 4-5 adapters drafted first |
| 9j | Additional sector adapters (Pharma, Defense, Mining, Industrial, Semi/AI-Infra) | User-authored as needed |
| 14 | Thesis repository inside FT (long-form per-holding) | ✅ Shipped 2026-05-17 — Detail page Thesis section gains 📄 In-app thesis subsection with goldmark render + textarea editor with live preview + Save / Save-as-new-version + history modal. Migration 0021 + reuses scorecards.Render. |
| 15 | Investment strategy / allocation framework | Depends on Spec 12 D2 ✓ |
| 16 | Alert strategy overhaul (851-alert problem) | Needs alert-noise audit first |
| 13 (numbering collision) | "Test coverage" (v2 doc) vs "Score automation engine" (Spec 12). Rename one | Open |
| ~~12 D5g~~ | ~~EUR toggle on stocks P&L column~~ | ✅ Shipped 2026-05-18 — `$ / €` toggle in P&L header, persists via `pnl_currency` preference, converts via the existing FX snapshot |

## 13. Version history of this doc

| Version | Date | Notes |
|---------|------|-------|
| 1.0 | 2026-05-17 | Initial author at the close of Specs 9f + 9g build. Captures every spec 1 → 12 + 9b/c/c.1/d/f/g + 7 + 10 + 11 + 11b. |
| 1.1 | 2026-05-17 | Spec 14 (per-holding theses) shipped. Added migration 0021 + thesis endpoints + Detail-page UI block. |
| 1.2 | 2026-05-17 | Handoff package v2 — Spec 9f D9 Whitespace Watchlist view shipped (filter pills, "+ watchlist" affordance carrying sector pre-selection). Migration 0022 retagged ORCL → data_center_reits + WPM → precious_metals_gold per Sector_Holdings_Mapping_v1.1. Watchlist schema extension: `sectorUniverseId` accepted on create. |
| 1.3 | 2026-05-17 | Pharma adapter v1 (draft) added to Scorecards repo. Applies to pharma_metabolic + pharma_immunology + gics_healthcare. Status `needs-review` (5 open decisions in §7 of the source MD). LLY + ABBV worked examples both calibrate to 13/16. |
| 1.4 | 2026-05-18 | Spec 13 — Test coverage on alert / technicals / portfolio_risk / performance / sector_rotation packages. ~95 test funcs, `go test ./...` clean. Spec 12 D5g — `$ / €` toggle on stocks P&L column. `pnl_currency` preference; converts via FX snapshot. |
| 1.5 | 2026-05-18 | **5 new sector adapters** (Defense / Mining & Metals / Industrial Electrical / AI Infra & Semis / Cloud Infra) — all v1 drafts, status `needs-review`, with open decisions in §7 of each source MD. Applies-to-sector wiring drives the 📋 button across the Sector Rotation tab. **Watchlist Market column** — per-row open/closed badge resolved by ticker suffix (matches Stocks tab behaviour). **Per-tab CSV export** — `GET /api/export.csv?tab=stocks\|crypto\|watchlist` + ⬇ Download CSV button on each tab toolbar. Flat column shape; no FX denormalization needed (Crypto rows already carry both EUR & USD columns). |
| 1.6 | 2026-05-18 | **Spec 15 — Thesis Library** shipped. New top-level **Theses** tab (between Scorecards and Watchlist) backed by the user's private GitHub repo `cross_sector_research`. Drop-zone accepts `<TICKER>_v<N>_locked.md` + optional `_scoring_log.md` and pushes both in one commit via stored fine-grained PAT (`FT_GITHUB_TOKEN` in `/etc/ft/env`, scoped to one repo, Contents:write only). Sortable/filterable index of every locked thesis with score / version / locked-date / next-earnings columns. Earnings-revision trigger: 🟡 amber ≤14d, 🔴 red ≤3d, 📝 revision_needed post-earnings if earnings > locked_date. "Stocks owned with no thesis" gap report. Inline goldmark+bluemonday MD renderer. Background sync cron pulls every 5 min. Endpoints: `GET /api/theses`, `GET /api/theses/{id}`, `GET /api/theses/gaps`, `POST /api/theses/upload`, `POST /api/theses/sync`. Migration 0023 creates `theses_index` table (cache; GitHub remains source of truth). Re-labels the Spec 14 holding-detail external-thesis-link field from "Notion / Google Doc" → "GitHub — cross_sector_research". |
| 1.7 | 2026-05-19 | **Spec 15 polish batch** — six incremental changes from the live-use shakedown of the Thesis Library: (1) **gap report split** into separate OWNED vs WATCHLIST sections (no more "+N more" truncation); (2) **multi-segment doctrine** — parser accepts `> **Primary Adapter:**` (RR.L pattern) in addition to `> **Adapter:**`; (3) **scoring-log-only uploads** — new endpoint `POST /api/theses/scoring-log` for methodology refreshes without a thesis attached, plus browser-suffix-tolerant filename matching (handles `_scoring_log (3).md` etc); (4) **Asset-Hedge framework parser support** (first piece of Spec 9i implementation, lives at `cross_sector_research/ft_specs/FT_Spec_9i_Three_Framework_Integration_v1.md` as a draft) — `> **Framework:** Asset-Hedge Scorecard` header line, `Instrument Type:` fallback for sub-type, new `asset_hedge` folder slug; (5) **per-framework score thresholds** — /16 passes at ≥12, /8 passes at ≥5 (Spec 9i calibration ladder), default ≥75% otherwise; (6) **keyword-based adapter routing fallback** — distinctive substrings (`semi`, `pharma`, `defence`, `oil`+`gas`, `precious`+`metals`, etc.) route correctly even when Gemini/Claude phrases an adapter name in a new way; (7) **Theses table grouped by ownership** — `theses_index` rows tagged `owned`/`watchlist`/`other` via JOIN to stock_holdings + watchlist; tab renders three stacked sections (OWNED green, WATCHLIST amber, OTHER dim if non-empty) so monitoring vs candidates are visually separated. Cross-sector research repo now has 9 locked theses: RHM.DE 14/16, ASML & LLY 12/16, ABBV/CLS/NVDA/RR.L/MTZ 11/16, GLD 5/8 (first Asset-Hedge). |
| 1.7.1 | 2026-05-19 | **Process change** — Master Spec is now bumped on **every FT commit** going forward (per user request 2026-05-19). Convention added to §7. Patch (v1.7.x) for polish, minor (v1.8) for new features, major (v2.0) for breaking architecture changes. Captures one-line "what changed" rationale per commit so the spec stays an honest record of how FT got to its current shape. |
| 1.7.2 | 2026-05-19 | **Process refined** — bump cadence relaxed from "every commit" to "every completed section / spec / sub-phase / polish batch / architectural decision." Typo fixes and single-line tweaks no longer trigger their own bump; they roll up into the next section bump. Same versioning scheme. §7 updated. |
| 1.7.3 | 2026-05-19 | **Off-site DB backup to Cloudflare R2.** Daily backup script (`deploy/backup-db.sh`, run 03:15 UTC via `/etc/cron.d/ft-backup`) now extends past the existing 14-day local rolling snapshot to also push to R2 bucket `ft-backups` via rclone. 90-day retention on the R2 side handled by `rclone delete --min-age 90d` at the end of each run. Credentials stored at `/var/lib/ft/.config/rclone/rclone.conf` (mode 0600, ft:ft). Fine-grained R2 API token scoped to one bucket (Object Read & Write). R2 upload failures log but don't fail the cron — local backup remains the primary recovery surface. Restore path: `rclone copy r2:ft-backups/ft-YYYY-MM-DD.db /var/lib/ft/ft.db.restored` + `systemctl stop ft` + swap files + `systemctl start ft`. |
| 1.7.4 | 2026-05-19 | **Weekly jarvis-config backup to R2** (`deploy/backup-jarvis-config.sh`, 03:30 UTC Sundays as root via `/etc/cron.d/ft-backup`). Tarballs everything needed to rebuild jarvis on a fresh Ubuntu box: `/etc/ft`, `/etc/ft-bot`, `/etc/cloudflared` (tunnel cert + named-tunnel JSON — the **most irreplaceable items**), `/etc/cron.d/ft-backup`, custom systemd units (ft / ft-bot / hct / cloudflared), `/var/lib/hct` (DB + attachments), `/var/lib/ft/.config/rclone/rclone.conf`, `/opt/ft-bot/*` (source not in git), `/opt/hct/*` (binary + state). Plus `packages.txt` (dpkg selections) and `os.txt` (uname + lsb_release). Includes a generated `RESTORE.md` with the 11-step rebuild walkthrough. Lands at `r2:ft-backups/jarvis-config/jarvis-YYYY-MM-DD.tar.gz`, 365-day retention. Combined with v1.7.3 daily DB backup = complete rebuild recipe. `deploy.sh` now also syncs `/etc/cron.d/ft-backup` from `deploy/ft-backup.cron` so cron stays git-tracked. |
| 1.7.5 | 2026-05-19 | **CSV import support** (`internal/persistence/csv_import.go`). The existing `POST /api/import/preview` endpoint now accepts `.csv` files in addition to `.xlsx`. Routes by filename extension. CSV schema matches the v1.5 per-tab export (`GET /api/export.csv?tab=stocks\|crypto`) so download → edit → re-upload is symmetric. Header row detects stocks (`ticker` + `invested_usd`) vs crypto (`symbol` + `quantity_held`). Single CSV upload populates only one kind; the other side stays untouched in the apply step. Same lenient parsing as xlsx (empty cells = NULL, `$/€/,/whitespace` stripped before ParseFloat, unknown columns ignored). Same slam-replace apply semantics as xlsx — preview diff shows additions/deletions before commit. Frontend import modal accepts `.xlsx,.xls,.csv`; ⌘K menu label updated to "Import xlsx / csv". |
| 1.7.6 | 2026-05-19 | **Import demote-to-watchlist** — stocks missing from a holdings import are now **moved to the watchlist** (not deleted). Carries ticker, company name, sector, sector_universe_id, current_price, thesis_link forward to the watchlist row. Sets a descriptive note ("Auto-moved from holdings on YYYY-MM-DD — removed via import (was invested $X)"). Idempotent: skips silently if the ticker already has an active watchlist row, so re-imports don't duplicate. Confirms what user explicitly wanted: (a) import never modifies watchlist entries (always true — they're separate tables), and (b) previously-held tickers that drop out of the import keep their context on the watchlist instead of vanishing. Preview modal shows a "↘ Will move to Watchlist (N)" panel before the user clicks Apply; completion screen confirms what moved. New store method `DemoteHoldingToWatchlist`. Crypto is unchanged for now (no demote pathway). |
| 1.7.7 | 2026-05-19 | **Import preview: stocks ↔ crypto independence made obvious.** When the imported file contains no rows for a kind (e.g. stocks-only CSV), the preview modal now shows a clear "✓ Your existing crypto will not be modified — this file contains no crypto data" panel for the absent section, with the checkbox + count chips + sheet tag hidden entirely. Previously the absent section showed a vague "No rows detected" message which read as worrying. No behaviour change — the data path was already safe (frontend defaults applyCrypto=false when cryptoCount=0; backend has `len(pending) > 0` guards) — just clearer communication. CSS adds an `.absent` modifier with dashed border + green-tinted success message. |
| 1.8.0 | 2026-05-20 | **Spec 9e Phase 1 sub-phase A — Crypto Indicators tab foundation.** New top-level **Crypto Indicators** tab between Crypto and Performance. Migration 0024 adds 5 tables (`crypto_indicators` seeded with 11 Phase 1 indicators, `crypto_indicator_snapshots`, `crypto_composite_snapshots`, `crypto_indicator_weights` seeded 25/20/35/20, `cowen_risk_manual`). New `internal/cryptoindicators/` package: scoring engine (linear / step / level_and_trend / trend_only function types, JSON-defined thresholds in `definitions/crypto_indicators.json`), composite engine (weighted bucket mean + redistribution + action band mapping: ≥60 strong_accumulate, ≥20 accumulate, >-20 neutral, >-60 caution, else distribute_wait), tooltips dictionary (layman language per indicator). New endpoints `GET /api/crypto-indicators` and `GET /api/crypto-indicators/composite/latest`. Tab UI: composite hero with -100..+100 scale bar + band label + 4 sub-score chips, 4 bucket sections with simple indicator cards (Phase 3 will replace with proper SVG gauge + 240×180 card grid). Sentiment bucket surfaces live F&G via existing `/api/feargreed` endpoint. **No data providers yet** — current_value/current_score columns are NULL until v1.8.1 (Phase 2 — FRED, DefiLlama, Farside, ISM, Cowen log-band) wires fetchers. Indicators render with "awaiting data" pills. F&G stays on Crypto News (already moved there by Spec 12 D10a, predating Spec 9e). |
| 1.8.1 | 2026-05-20 | **Import: invalidate Summary + Watchlist caches on done.** Previously the import completion screen only invalidated `state.stocks` and `state.crypto`, leaving `state.summary` and `state.watchlist` cached. Result: a user who navigated to Summary right after an import saw pre-import totals until a manual reload — which read as "the totals are wrong" even though the DB was correct. Adds both to the invalidation set in the import-done handler. No backend / DB / API change. (Spec 9e Phase 2 — data providers — bumps the next minor version, not the next patch.) |
| 1.8.2 | 2026-05-20 | **Spec 9e Phase 1 sub-phase B (part 1) — 5 indicators light up.** `internal/cryptoindicators/providers/` package added with three providers: `fred.go` (DGS2 + DTWEXBGS via api.stlouisfed.org with embedded 4w trend computed from the 60-obs window), `coingecko.go` (BTC dominance from `/global`, ETH/BTC ratio from `/simple/price`, BTC spot for snapshot row), `feargreed.go` (wraps existing `internal/market.FetchFearGreed`). New `refresher.go` orchestrates the sweep — fetches each provider sequentially with an 800ms gap, scores via the existing engine, upserts into `crypto_indicators`. Trend_4w for CoinGecko/F&G is computed from local snapshot history (`PriorSnapshotValue` SQL query, ±2 day window); stays NULL until 28 days accumulate. New endpoint `POST /api/crypto-indicators/refresh` triggers the same sweep on demand. Daily 00:30 UTC cron in `cmd/ft/main.go` runs `RefreshAllAndSnapshot` (refresh + write `crypto_indicator_snapshots` + `crypto_composite_snapshots`); also a 30s post-boot warm-up refresh so the tab has data on first visit. Frontend gains a `⟳ Refresh now` button next to "Auto-syncs daily at 00:30 UTC" hint. New config: `FRED_API_KEY` (env var) + `FT_CRYPTO_INDICATORS_DATA_DIR` (default `/var/lib/ft/data`). Indicators NOT wired in v1.8.2 (Cowen log-band, price_vs_200wma, risk_indicator, Farside ETF flow, DefiLlama stablecoin supply, ISM) keep showing "awaiting data"; v1.8.3 fills them. |
| 1.8.3 | 2026-05-20 | **Spec 9e Phase 1 sub-phase B (part 2) — remaining 6 indicators wired, all 11 now scoring.** Migration 0025 adds `btc_price_history` (cache table, ~3500 daily closes back to 2013, one-shot seed from CoinGecko `/coins/bitcoin/market_chart?days=max`). New `btc_history.go` runs the Cowen math: log-linear regression on log10(close) vs log10(days since CG genesis) → residual classified into quintiles (lower/mid_lower/mid/mid_upper/upper) for `cowen_log_band`; 200-week MA via weekly bucketize → `cowen_price_vs_200wma`; risk proxy = mean(normalised log_band, normalised price/200wma) for `cowen_risk_indicator` (2-input proxy per Spec 9e §D8 — MVRV-Z deferred to Phase 3). New providers: `defillama.go` (stablecoin supply with 4w ROC computed directly from `/stablecoincharts/all`), `farside.go` (BTC spot ETF flow CSV/HTML scrape with defensive parsing; HTML fallback if CSV fails), `ism.go` (reads `/var/lib/ft/data/ism.json` + helpers SaveISM/ValidateISM). `universal_stablecoin_supply` definition switched from `linear` to `trend_only` so it scores by 4w ROC. CoinGecko client now retries on 429 with 2/4/8s exponential backoff (fixes the warm-up rate-limit issue from v1.8.2). New endpoints `GET /api/crypto-indicators/ism` + `POST /api/crypto-indicators/ism` (multipart or JSON body). Frontend gains a 📊 Update ISM data… button that file-picks a JSON, posts it, fires a refresh. `pal_ism` stays "awaiting data" until user uploads the 12-month seed. |
| 1.9.0 | 2026-05-21 | **Earnings-revision trigger** — three-tier notification when a thesis becomes revision-needed (earnings published since the thesis was locked). (1) **Visual nav badge:** Theses tab shows `⚠ N` count when `theses_index.earnings_urgency='revision_needed'` rows > 0. (2) **Telegram push:** `handleBotAlerts` now scans `theses_index` and emits `thesis_revision_needed` alerts for tickers held in `stock_holdings`. The existing bot cron (`@FinsFTAlerts_bot` 13:00/17:00/21:00 UTC) picks these up automatically — no bot deploy needed. Same `notification_log` dedup, ack via existing `/done`. (3) **Revision prompt generator:** new endpoint `GET /api/theses/{id}/revision-prompt` returns a markdown LLM prompt populated with current thesis content + pillar scores + explicit revision instructions for Gemini/Claude/etc. Per-row 📋 button on revision-needed theses opens a modal with copy-to-clipboard. **Detection cadence improved:** new `RefreshEarningsOnly` in `internal/refresh/earnings_only.go` runs hourly — slim Yahoo calendar refresh that only updates `earnings_date`/`ex_dividend_date`. Total practical lag from earnings release → notification: ~3-4 hours (was 12-18 hours under daily 04:00 UTC cadence). |
| 1.9.1 | 2026-05-21 | **Pal business-cycle indicator: ISM → CFNAI (auto from FRED).** Migration 0026 drops `pal_ism` (which required monthly manual JSON upload via the 📊 button) and inserts `pal_cfnai` (Chicago Fed National Activity Index — same monthly cadence, same business-cycle pulse signal Pal uses ISM for, free + automated via FRED `CFNAI` series). New scoring thresholds reflect CFNAI's signed scale: ≥0.2 + rising → +1.0, ≥0 + rising → +0.5, ≥0 + flat → 0, ≤0 + falling → -0.5, ≤-0.7 + falling → -1.0 (recession likely). Refresher swaps the ISM JSON read for a FRED series fetch (third CFNAI call alongside DGS2 + DTWEXBGS). Frontend: removes the 📊 Update ISM data… button. Tooltip updated to explain CFNAI as the Pal-equivalent. All 11 Phase 1 indicators now fully automated — zero manual maintenance for indicator data. The `internal/cryptoindicators/providers/ism.go` reader + `POST /api/crypto-indicators/ism` endpoint are kept (cheap) in case a future hybrid (CFNAI default + manual ISM override, à la Cowen Risk) is wanted. |
| 1.9.2 | 2026-05-21 | **News tabs unblocked + crypto news provider swap.** NewsAPI key installed on jarvis (Market News tab now populating). CryptoPanic's free Developer tier was discontinued 2026-04-01 (cheapest paid is $50/wk → not worth it for personal use); swapped `internal/news/crypto.go` from CryptoPanic to **CryptoCompare News API** — free, no API key needed, ~100k req/month limit, same publisher mix (CoinDesk, Cointelegraph, The Block, Decrypt, etc.). Article shape unchanged; sentiment now derived from CryptoCompare's upvotes/downvotes (most articles 0/0 → neutral, which the UI already handles). Source slug `cryptopanic` → `cryptocompare` in the handler. Legacy `CRYPTOPANIC_API_KEY` env var still parsed by config (unused, no breakage). |
| 1.9.3 | 2026-05-21 | **Crypto news: RSS aggregation (third time's the charm).** CryptoCompare turned out to also require an API key now — "free" tier needs registration. After two rugged providers in one day, pivoted to direct RSS reads from publisher feeds: **CoinDesk, Cointelegraph, The Block, Decrypt**. No third-party aggregator, no API keys, no rate limits, no free-tier-discontinued risk. The publishers publish RSS and actively want it crawled. Generic RSS 2.0 parser via `encoding/xml` stdlib; multi-format `pubDate` tolerance; partial-failure handling (one bad feed doesn't kill the aggregate); deduped by URL; capped at 50 newest articles. Real-browser User-Agent + redirect-follow handles CoinDesk's Arc CDN. Sentiment is "neutral" for every article (RSS doesn't carry sentiment data — UI already handles neutral). Handler source slug `cryptocompare` → `rss`. Adding/removing a publisher is a one-line append/delete in the `cryptoFeeds` slice. |
| 1.10.0 | 2026-05-21 | **Spec 9k Phase A — Political & Insider Signal tab MVP (insider Form 4).** New top-level **Signals** tab between Crypto Indicators and Performance. Migration 0027 creates all five Spec 9k tables (`signal_events`, `legislators`, `committee_assignments`, `committee_sector_map`, `eo_sector_keywords`) — only `signal_events` is used in this phase; the rest are scaffolded for v1.11.0. New `internal/signals/` package: `universe.go` (InUniverse filter with 30-min cache covering stock_holdings + watchlist + sector_universe ETFs), `tiers.go` (insider tier computation + cluster-buy ALARM promotion: ≥3 distinct actors / 14d / same ticker / BUY), `insider.go` (SEC EDGAR Form 4 ingest — ATOM feed → per-filing index → form4.xml; ~7.7 req/sec self-limit per SEC fair-access policy; polite User-Agent), `query.go` (List/Counts/Acknowledge). Spec 9k §3 locked thresholds: BUY $25K / SELL $100K / CEO-CFO BUY any size / CEO-CFO SELL $250K. Endpoints `GET /api/signals?tier=&type=&range=&include_acked=`, `POST /api/signals/{id}/ack`, `POST /api/signals/refresh-insiders` (debug manual trigger), `GET /api/signals/universe` (debug snapshot). Daily 23:00 UTC cron (after sector_rotation 22:00) wired in `cmd/ft/main.go`. Frontend: tier/range/type chip filters, sortable signal table, per-row ack button, nav-bar `🔴 N` alarm badge. Telegram routing + Congress/EO ingest follow in v1.11.0 (9k.B) and v1.12.0 (9k.C). |
| 1.10.1–5 | 2026-05-21 | **Spec 9k Phase A — same-day stabilisation pass.** Five small post-deploy fixes rolled into the live tab: (1) **gzip fix** — `Accept-Encoding` header was set manually, which disabled `net/http`'s auto-decompression; XML parser silently choked on raw gzip bytes. (2) **ATOM `<category>` filter** — EDGAR's `type=4` query leaks other forms (497J, 4/A); now hard-filtered. (3) **Charset reader** — SEC ATOM declares `encoding="ISO-8859-1"`; switched to `xml.NewDecoder + charset.NewReaderLabel` (golang.org/x/net/html/charset). (4) **Per-filing fetch via index.json** — SEC's actual XML name is per-filing (`wk-form4_NNN.xml`), not `form4.xml`/`primary_doc.xml`. One JSON discovery + one XML fetch replaced the old three-attempt waterfall, halving total ingest time. (5) **Async manual refresh** — the synchronous endpoint blew the reverse-proxy's 30s gateway timeout (ingest takes ~40s); now returns 202 and runs in a goroutine, frontend polls `/api/signals` every 10s for new rows. (6) **Universe + company-name enrichment** — added universe-class filter chips (💼 Owned / 👁 Watchlist / 🏷 Sector ETF / 🌐 Unowned), per-row badge in the ticker cell, company name (from Form 4 `<issuerName>`, migration 0028), and sector resolved at read-time from holdings/watchlist/sector_universe. |
| 1.11.0 | 2026-05-21 | **Spec 9k Phase B — Congress + Executive Order ingest + full UI.** Migration 0029 seeds the committee→sector jurisdiction map (13 committees: House/Senate Armed Services, House Energy & Commerce, Senate Commerce, Senate/House Banking, Senate Energy & Natural Resources, House Natural Resources, House Agriculture, Senate HELP, House Veterans, House/Senate Intelligence) plus a starter set of ~60 EO sector keywords across Defense, Semiconductors, AI/GPUs, Energy, Nuclear, Power/Grid, Pharma, Critical Minerals, Comms, Data Center, Software/Cloud. Three new ingesters: `internal/signals/congress.go` (House Stock Watcher + Senate Stock Watcher JSON aggregators, 90-day lookback, fuzzy legislator name resolve, committee-jurisdiction ALARM tier compute), `internal/signals/eo.go` (Federal Register API, keyword pass + company-name pass; ALARM when EO names a held company, FLAG when EO matches a sector with active holdings/watchlist), `internal/signals/legislators.go` (quarterly refresh from `unitedstates/congress-legislators` for `legislators` + `committee_assignments` — current 535 active reps/sens). Tier compute extended (`tiers.go` adds `CongressTier(CongressEvent, Thresholds)`). Three new endpoints: `POST /api/signals/refresh-congress`, `/refresh-eo`, `/refresh-committees` (all 202-async). Crons: Congress 23:10 UTC, EO 23:20 UTC, quarterly legislator refresh 02:00 UTC on first day of Jan/Apr/Jul/Oct. Frontend: four refresh buttons (Insiders/Congress/EO/Committees), EO title rendered in the Ticker column when ticker is null (sector-only matches), `notes` field returned in API. Telegram routing + Phase C polish ship in v1.12.0. |
| 1.11.1 | 2026-05-21 | **Signals tab usability + Settings maintenance checklist.** Three polish changes after v1.11.0 first-use feedback. (1) **Signals page restructured into three sections** (Insider / Congress / EO) instead of one flat table; each section has its own sortable column headers (Date / Tier / Ticker / Actor / Amount) — click to toggle asc/desc, click a different column to switch sort axis. Section row-counts in the header. (2) **⟳ Refresh all signals** button — single click fires all four endpoints (insiders + congress + EO + committees) in parallel, status text shows what launched + a live "still running" counter; individual triggers moved into an expandable `<details>` block. (3) **Settings → Maintenance checklist** — new section with 9 recurring tasks (master spec sync, quarterly committee verify, EO keyword false-positive review, committee-jurisdiction audit, API key rotation, DB backup, holdings audit, thesis-revision scan, sector universe review), each with cadence + detail + a localStorage-backed "last done" timestamp + ✓ done / ↻ redo buttons. List is editable by editing `MAINTENANCE_ITEMS` in app.js — no backend storage. |
| 1.22.0 | 2026-05-29 | **Spec 9l Phase 1 baseline — Crypto Thesis Framework.** Migration 0031 creates 8 new tables (`crypto_adapters`, `crypto_adapter_versions`, `crypto_theses`, `crypto_thesis_history`, `crypto_thesis_dependencies`, `cascade_events`, `crypto_allocation_current`, `crypto_allocation_history`). New `internal/cryptotheses/` package (`types.go` + `service.go`): enum suite (AdapterType, ScorecardType, Band, HoldingHorizon, BTCBeta, Q5Mechanism, Status, DependencyType, CascadeStrength); `PillarScores` handling both 9-Q /18 alts and 6-pillar /12 BTC; `Q5Detail` structured triple (mechanism + annual_usd + fdv_usd → computed accrual_pct); `ComputeBand` / `ComputePillarPassGate` / `ApplyPassGate` / `RecommendedActionFromDelta`; goldmark + bluemonday Render; full Adapter CRUD with version archive on save-as-new-version. New `internal/server/crypto_adapters_handlers.go` exposes 7 endpoints under `/api/crypto/adapters/*` parallel to Spec 9g pattern. Frontend: `Theses` tab renamed `Stock Theses`; new sibling `Crypto Theses` tab next to Crypto Indicators; two-pane Repository view with status icons + /12 vs /18 scorecard chip + kill-criteria collapsible + edit modal with Save-as-new-version toggle + version-history modal. `SeedIfEmpty` seeds 8 adapters as `v0-placeholder` drafts. `crypto_allocation_current` seeded 60/10/5/5/20 default split. v0.2 spec patch drafted (closes open items #4 scoring-log columns, #6 drift threshold ±15%, #7 allocation panel two-table model, #8 Spec 9m placeholder; promotes cascade rules into spec body). |
| 1.22.1 | 2026-05-29 (PM) | **Spec 9l v0.3 patch — adapter MDs + calibration triad land.** Migration 0032 adds BEFORE INSERT/UPDATE OF trigger pair on `crypto_theses` enforcing Speculative-adapter horizon restriction (blocks Never-Sell/Cycle/Multi-year, allows Trade/Medium/TBD); RAISE(ABORT, ...) with explicit message that the API layer can match for 422 translation. 3 adapter MDs loaded into `crypto_adapters` and locked at `v1` via one-off seeder (`cmd/ft-seed-9l/main.go`): BTC (6-pillar Monetary /12), L1 (9-Q template for all /18 adapters, ETH is reference case), Speculative (strictest adapter with mandatory 5% per-thesis / 15% aggregate position caps + Trade/Medium-only horizon). 3 calibration-anchor theses loaded into `crypto_theses` via second one-off seeder (`cmd/ft-seed-9l-theses/main.go`): BTC v1 (6/12 Hold, Never-Sell), ETH v1 (13/18 Strong, ceiling anchor, Multi-year, Q5=fee_burn $700M/$252B=0.28%), LUNC v1 (3/18 Exit, floor anchor, Trade, multi-VETO via Do Kwon 15yr sentence + 99.99% drawdown, Q5=buyback $17.5M/$526M=3.33%, pillar_pass_gate_failed=1). 5 remaining adapter MDs (l2, defi, infra, depin, rwa) still draft v0-placeholder — pending Claude.ai authoring per kickoff Step 4 stop. ETH v1 internal Q2 contradiction flagged in v0.3 patch §B for product-owner reconciliation (header total 13 requires Q2=2; detail prose rounds to Q2=1). D12 thesis CRUD endpoints, D17 liquidity pre-filter, D18 secondary sanity-check, D19 cascade-flag, D25 Scoring Engine, D26 cross-thesis table, D27 detail page, D29 Allocation UI, D30 9e sell-window verdict all remain Phase 2 — sequencing waits for L2 adapter MD landing. |
| 1.30.0 | 2026-06-01 (nineteenth pass) | **SC-17 Phase 1 — eToro statement importer (annual + YTD performance history).** Off `FT_Build_Handover_SC17_v3_supplement_2026-06-01.md`. First eToro subsystem; namespaced `etoro_*` / `/api/etoro/*` to stay clear of the unrelated Spec 9d `performance` trade-analytics namespace. **Propose-and-apply; Phase 1 never touches holdings** — produces a read-only performance panel from uploaded eToro account statements (.xlsx). **Migration 0038** adds two denormalized tables: `etoro_performance_year` (per-year authoritative summary — range/is_ytd/realised/dividends/fees/interest/net + computed-pnl + recon-delta in USD & EUR, superseded/source_file/imported_at) and `etoro_performance` (per (year, asset_type) breakdown with **disc/copy column pairs** so R1's strategy split survives without a null-bucket), each indexed `(user_id, year, superseded)`. New `internal/etoro` package: `Parse(r, fileName, now)` via excelize (requires Closed Positions + Financial Summary sheets; Account Summary + Dividends optional), normalises asset types (Stocks/ETFs/Crypto/CFDs), treats the **Financial Summary as the authoritative headline** (its P&L rows override per-asset realised totals; net = sum of every FS numeric row; **reconciliation delta** warns when \|EUR\| > 1). **R1 two-axis split:** discretionary vs copy is driven by Closed Positions `Copied From` (`-`/empty = discretionary incl. discretionary CFDs; a username = copy); **`Type=CFD` is a wrapper/display flag only**, never the strategy or asset-class signal. Store: `UpsertEtoroPerformanceYear` (transactional supersede-on-reupload) + `ListEtoroPerformance` (live rows newest-first + asset breakdowns). Three endpoints (token-or-cookie via `requireUser`): `POST /api/etoro/import/preview` (multipart .xlsx → parsed + staged in-memory keyed by userID, mutex + 10-min TTL), `POST /api/etoro/import/apply`, `GET /api/etoro/performance`. Frontend: a **Performance panel under the Summary tab** (`#etoro-perf-panel`) — empty-state + Upload affordance, else an **All / Discretionary / Copy** toggle (persisted to `localStorage` `ft.etoroStrategyView`), a YTD card, per-year tables (realised/dividends/fees/net/trades + Total row + recon flag), plus a two-step import modal (dropzone → preview → apply). Display currency follows the existing `display_currency` cookie / `state.summary.currency`. **Phase 2a (ISIN bootstrap) + Phase 2b (reconciliation) deferred.** `go build`/`go vet ./...` clean; migration 0038 verified on a throwaway DB copy (production untouched); `node --check web/app.js` + `make build` OK. Production DB delta vs v1.29.0: `schema_migrations` gains 1 row (0038); 2 new tables, both empty until first import. |
| 1.29.0 | 2026-06-01 (eighteenth pass) | **Watchlist parity batch — SC-14 + SC-16 + SC-15.** Same fix family as SC-10 — the Watchlist tab now matches Stocks/Crypto. **SC-14**: SCORE column re-pointed off the empty legacy `framework_scores`/`watchlist_frameworks` path onto `theses_index` (stocks) / `crypto_theses` (crypto) via `Store.ThesisScoresByTicker` + `Store.CryptoThesisScoresBySymbol` → `pickScore`/`pickCryptoScore` → new `Score *scoreSummary` on `watchlistRow`, rendered by the shared `scoreCell()` helper (🔒 lock / `/16` stock vs `/18 — band` crypto / stale / GitHub link); Score sort keys off `e.score.totalScore`. (theses_index has no band column — stock rows render no band, flagged.) **SC-16**: manual ticker-add enrichment — `handleCreateWatchlist` calls best-effort `enrichWatchlistEntry` (8s timeout, fills only blank fields) resolving company name + price (`market.FetchStockQuote`) + free-text sector (`market.FetchYahooProfile`; crypto `market.LookupCryptoBySymbol`). Enrich-and-flag (D16.2): never guesses `sector_universe_id` (left NULL); UI shows `watchlistGapChips` "needs data"/"needs sector". **SC-15**: flat ↔ by-sector view toggle (segmented control persisted to `localStorage` `ft.watchlistGroupBy`); grouped mode buckets rows by `sector_universe_id` into collapsible sections (`watchlistGroups`: caret + name + count) ordered by `user_sector_ordering` else alphabetical, "Unsectored" last; within-group order preserves the column sort; flat mode unchanged. Sector names/order from existing `/api/sector-rotation/metrics`. No schema change / no migration; Go change confined to `internal/server/watchlist_handlers.go`. `go build`/`go vet` clean; `node --check web/app.js` + `make build` OK. |
| 1.28.0 | 2026-06-01 (seventeenth pass) | **Spec 9p P1+P2 — Macro Regime & Sector Rotation.** New `internal/macroregime` package (deterministic growth × inflation quadrant classifier; reuses the Pal/9e `providers.FREDClient` — does not rebuild the FRED layer). Migration 0037 adds 5 tables (`macro_indicators`, `macro_indicator_snapshots`, `macro_regime_history`, `regime_playbook`, `ism_manual`) + seeds 38 §F doctrine rows (Q1=8/Q2=9/Q3=11/Q4=10). 13 FRED series (empire/philly/dallas regional-Fed, INDPRO, UNRATE, ICSA, CPI headline+core YoY, FEDFUNDS, M2 13-wk RoC, T10Y2Y curve, HY OAS credit, DTWEXBGS dollar) classified by rate-of-change into Q1 Goldilocks / Q2 Reflation / Q3 Stagflation / Q4 Deflation, plus augmenting state (rates/liquidity/curve/credit/dollar) + thematic flags + confidence. ISM via regional-Fed nowcast + manual override (35-day staleness gate). 6 endpoints under `/api/macro/*` (regime read with indicators+sparkline+playbook+ISM+suggested-Jordi; refresh; ISM set; playbook list/upsert/delete). Nightly 01:00 UTC cron (RefreshAllAndSnapshot) + 45s warm-up RefreshAll. Sector Rotation tab **retitled "Macro Regime & Sector Rotation"** — collapsible macro band on top (indicator cards w/ RoC + sparkline, regime banner, ISM input, playbook favoured/neutral/avoid columns) above the existing 34-row sector table; **divergence overlay** maps sector rows → playbook stance (✓Favoured/○Neutral/✕Avoid + ⚠ holding-a-disfavoured-sector). **Suggest-a-Jordi-regime is suggest-only** (Apply posts to existing `/api/regime/jordi`, never auto-set). "Not investment advice" footing throughout. P3 deferred. `go build`/`go vet` clean; migration verified on throwaway DB copy; `node --check web/app.js` + `make build` OK. |
| 1.27.0 | 2026-05-31 (night, sixteenth pass) | **Thesis authoring parity — SC-12 (crypto drop-box → parse → pre-fill D25) + SC-13 (stock guided form → strict MD → existing upload).** Off `FT_Build_Handover_SC12-13_2026-05-31.md` + `FT_Locked_Thesis_File_Format_Spec_Crypto_Supplement_v1.md` (Claude.ai). **Frontend-only — no Go, no schema, no DB delta vs v1.26.0.** **SC-12 (crypto, DB-first preserved):** the Crypto Theses repository view gains a drop zone (`#d25-ingest-zone`, click-or-drop a `.md`) that parses a crypto locked-thesis and pre-fills the **existing** D25 create form — NOT auto-create, NOT auto-lock (D12.1); the user reviews + locks via the normal path so cascades fire as today. New parser `parseCryptoLockedThesis(text)` reads the full structure per the crypto supplement: title regex (`# <SYM> — <Name> — Locked Thesis v<N>`, em-dashes), required `> **Final Score:** N/M`, Adapter line primary slug + `` `sub-type` `` in backticks **plus** the hybrid `+ <Adapter2> (\`sub2\` … secondary)` round-trip (HYPE/XRP/HBAR), Holding Horizon, BTC-Beta, Parent Chain, per-pillar `### Q<n> … **score**` headers cross-checked against the `**Rounding:** … → **score.**` line, and Q5 named mechanism (→ 14-value enum). **Parse-confident-or-blank-and-flag, never infer a score (D12.2)** — unparseable fields render blank + flagged in an ingest banner. **Sub-count insight:** MD pillar sub-row counts do NOT match `D25_SCHEMA_BY_ADAPTER` (e.g. XRP l1 Q1 = 4 MD subs vs schema 3), so the parser trusts only the pillar **score** and synthesizes the form's subCriteria as `new Array(def.subs.length).fill(score)` — all-equal deterministically rounds back to `score` via `d25ComputePillarScore`, guaranteeing a **green reconciliation badge** (`d25ReconRowHTML`: computed total vs parsed Final Score, red with delta on mismatch). Hybrid round-trip required a one-line fix to `d25BuildPayload` (now emits `secondaryAdapterSlug`; backend `thesis_write_service.go` already accepted it). **Adapter-name surprise flagged:** the supplement writes `infrastructure` but the D25 slug is `infra` (mapped via `CRYPTO_ADAPTER_NAME_TO_SLUG`). Acceptance test passes offline — the six SC-11 theses (HYPE 11/18, XRP 11/18, OP 8/18, HBAR 10/18, ADA 8/18, SUI 7/18) all parse GREEN; `XVM_v1_prefilter_block.md` is correctly rejected by the title regex (no "Locked Thesis vN" — it's a pre-filter rejection, not a thesis, must NOT enter `crypto_theses`). Existing manual D25 form unchanged (D12.3). **SC-13 (stock, GitHub-first preserved):** the Stock Theses drop zone gains a **✍️ Guided form…** button (`#dz-guided`) opening a modal that serialises fields → strict-format MD via `serializeStockThesisMD(d)` → the **existing** `POST /api/theses/upload` (no new table, no DB-first lifecycle — D13.1). Live `#st-preview`; Patterns A (operating /16, `(\`subType\` sub-type — new)`), B (asset-hedge Framework + Instrument Type /8), C (explicit Framework); em-dashes U+2014, `>`-prefixed metadata, standalone `> **Final Score:** N / MaxN — verdict` all guaranteed by the serializer. Adapter validated against `STOCK_ADAPTERS` before upload — unknown adapter warns in the form rather than failing the upload (S-13a). Existing drop box unchanged (D13.2). Shared contract: SC-13 writes the format spec, SC-12 reads it. New CSS (`d25-ingest-zone`/`.dragover`, `d25-ingest-banner`, `d25-recon-badge`/`.ok`/`.err`, `modal-wide`, `st-grid`, `st-preview`). `node --check web/app.js` clean; offline parser + serializer harnesses green; `make build` OK; `/healthz` green post-deploy. Remaining handover queue: Spec 9p P1+P2 (macro regime panel + playbook/divergence overlay). |
| 1.26.0 | 2026-05-31 (night, fifteenth pass) | **SC build series — SC-05 (Cowen weekly-capture autofill from live 9e indicators + suggest-only classification).** Fourth batch off `FT_Build_Handover_SC01-10_Spec9p_2026-05-31.md` (Claude.ai). **Frontend-only — no Go, no schema, no DB delta vs v1.25.0.** The Cowen Sunday-capture modal (`openCowenCaptureForm`) gains a primary **⤓ Pre-fill from live indicators** button; the existing **↻ Pre-fill from last submission** drops to a secondary ghost button. New `cowenPrefillFromLive()` reads `GET /api/crypto-indicators` (Spec 9e) and maps the latest readings into the form: `btc_vs_200wk_ma_pct` ← `cowen_price_vs_200wma` (ratio → % via `(v−1)×100`); `btc_vs_200d_ma_pct` ← computed client-side from `GET /api/crypto-indicators/btc-history?days=205` (latest close vs 200-day SMA — no dedicated 200d indicator exists); `log_band_third` ← `cowen_log_band` score mapped from the indicator's 5-band scale to the form's 3 thirds (score ≥0.5 → lower, ≤−0.5 → upper, else middle — the indicator exposes only a residual value + score, not the band string); `risk_indicator` ← `cowen_risk_indicator` (0–1 passthrough); `btc_dominance_pct` + `btc_dominance_4wk` ← `cowen_btc_dominance` value + `trend4w` (±0.5% flat band → rising/flat/falling, matching the server's `trendMatches`); `eth_btc` + `eth_btc_4wk` ← `cowen_eth_btc`. **Suggest-only (AC2)** — nothing persists until "Classify + save"; the authoritative regime is still computed server-side by the unchanged `POST /api/regime/cowen/auto`. A live **regime-preview bar** (`updateCowenRegimePreview` — a JS mirror of `regime.ClassifyCowen`, using the prior submission's cycle phase as `priorCyclePhase`) recomputes on every form input/change so STABLE/SHIFTING/DEFENSIVE/UNCLASSIFIED updates as the user edits; a suggested cycle phase is pre-selected from the risk indicator (>0.75→4, ≥0.5→3, ≥0.25→2, else 1) but stays editable (handover §96 — final cycle-phase + macro-flag call stays the user's). **MVRV-Z always left blank** for manual entry (AC3 — no free source). **Staleness gate (AC4)** — any indicator whose `updatedAt` is >10 days old leaves its field blank rather than seeding a stale value; the suggestion note lists which fields were filled vs left blank. **Schema surprises flagged, not silently corrected**: form's 3-way `log_band_third` vs the 5-band 9e indicator (mapped via score); no 200d-MA indicator (computed from BTC history). New CSS (`cw-prefill-row`, `cw-suggest`, `cw-preview-bar`, `cw-regime-val`). `node --check web/app.js` clean; `make build` OK; `/healthz` green post-deploy. Snags carried: S1, S2. |
| 1.25.0 | 2026-05-31 (night, fourteenth pass) | **SC build series — SC-09 (reusable contextual hover-explainers) + SC-06 (regime-toggle tooltips).** Third batch off `FT_Build_Handover_SC01-10_Spec9p_2026-05-31.md` (Claude.ai). **Frontend-only — no Go, no schema, no DB delta vs v1.24.0.** **SC-09**: new `FT_GLOSSARY` map in `web/app.js` (`term → {term, definition, calculation?}`) feeds a single shared floating popover (`ft-explainer-pop`). `explainerMark(key)` emits a small ⓘ trigger (returns `''` for unknown keys so value-derived keys are safe to drop in unconditionally); `wireGlobalExplainers()` attaches mouseover/mouseout/focusin/focusout handlers **once at boot via document-level event delegation** — so any `[data-explain]` rendered by any current or future view shows the explainer with zero per-render re-wiring (AC #3 satisfied structurally). Popover renders term + definition + optional Calc line. Seeded 6 base terms (`dist-to-sl`, `dist-to-tp`, `12m-vol`, `proposed-sl`, `score`, `vol-tier`) + 3 regime terms; ⓘ markers added to the Stocks-table headers Proposed SL / Dist to SL / Dist to TP / 12m Vol / Score (existing native `title=` tooltips on those headers retained as fallback). **SC-06**: the Jordi quick-set modal (`openManualRegimeModal('jordi')`) Stable/Shifting/Defensive buttons each carry the SC-09 ⓘ (`regime-stable`/`regime-shifting`/`regime-defensive`; the `unclassified` button has no copy and renders no marker) — the first SC-09 consumer (AC #2). Copy reflects the **live SC-08 two-tier proximity rule**: Stable = entry-zone (new-buy) alerts fire + proximity at 75%; Shifting = proximity tightens to 85%, entry alerts still fire; Defensive = new-entry/watchlist alerts silenced + proximity 85% + effective regime = the more defensive of Jordi and Cowen. New CSS (`ft-explain`, `ft-explainer-pop` + `fe-term`/`fe-def`/`fe-calc`). **Validation**: `node --check web/app.js` clean; `make build` OK; `/healthz` green post-deploy; served bundle contains the new component. Snags carried: S1, S2. |
| 1.24.0 | 2026-05-31 (night, thirteenth pass) | **SC build series — SC-08 (dual stop-loss methodology + two-tier proximity alerting).** Second batch off `FT_Build_Handover_SC01-10_Spec9p_2026-05-31.md` (Claude.ai). **Migration 0036** (`0036_stock_holdings_sl_method.sql`): two ALTERs — `sl_method TEXT NOT NULL DEFAULT 'vol_envelope' CHECK (sl_method IN ('technical','vol_envelope'))` + `sl_safety_pct REAL NOT NULL DEFAULT 0.02`; all 24 existing `stock_holdings` rows backfill to the defaults (verified on a DB copy). **Domain/store**: `StockHolding` gains `SLMethod *string` + `SLSafetyPct *float64`; `stockSelectCols` + `scanStock` carry the two columns; `UpdateStockHolding` SQL COALESCE-preserves both (omitted field = unchanged). **Effective-stop resolution** (`metrics.EffectiveSL(h)` → price/source/ok): manual `stop_loss` (>0) always wins, source `"manual"`; else the method computes — `technical` = support1 (else support) − `volTierMultiple(volTierAuto)`×ATR_weekly (low 1.5 / medium 2.0 / high 2.5 / extreme 3.0, mirrors the client recompute); `vol_envelope` = avg_open × (1 − vol12m/100 − safety[default 0.02]); returns ok=false (→ em-dash) when inputs missing. New `StockMetrics` fields `EffectiveSLPrice` / `EffectiveSLSource` / `DistanceToTPPct` / `ProgressToSL` / `ProgressToTP`; `DistanceToSLPct` + `RiskReward` re-keyed onto the effective stop. **Two-tier proximity family** (`internal/alert/proximity.go`) — replaces the old flat-margin `slProximity`/`tpProximity` (both deleted from `alert.go`; RSI + dist-to-SL RED/AMBER + green R/R retained). Progress-based + symmetric: `progressToSL=(entry−price)/(entry−SL)`, `progressToTP=(price−entry)/(TP−entry)`. AMBER fires at progress ≥0.75 (tightened to 0.85 when the effective regime is Shifting/Defensive); RED fires within 2% of the stop (`px ≤ SL×1.02`) or target (`px ≥ TP×0.98`); RED supersedes AMBER on the same side. Four distinct `notification_log` kinds (`proximity_amber_sl` / `proximity_amber_tp` / `proximity_red_sl` / `proximity_red_tp`) dedup independently of the RED/AMBER classifier. Long-only (Snag S6). `handleBotAlerts` resolves `currentEffectiveRegime` → `tightened` flag, loops stocks calling `alert.ProximityEvents(h, m, tightened)`, ack-checks `ev.Kind`; alerts surface through the existing OpenClaw `/api/bot/alerts` poll — **no bot deploy needed**. **API**: `PUT /api/holdings/stocks/{id}/sl-method` (validates method ∈ {technical, vol_envelope}; `store.UpdateStockHoldingSLMethod` sets `sl_method` + COALESCE-preserves `sl_safety_pct`). **UI** (`web/app.js` + `app.css`): new **Dist to TP** column (`distToTpCell` — green, brightens near/at target); Proposed SL now prefers `m.effectiveSlPrice`; per-row **Tech/Vol** stop-method toggle (`slMethodToggle`) under Proposed SL that PUTs the method then reloads (🔒 hint when source = manual); Edit modal gains **Stop method** + **Vol-envelope safety** fields (levels section, flow through `stockFromReq`); header tooltip on Proposed SL explains both methods. **Discipline**: supplements not in-place edits (proximity.go is a new file; `alert.go` only had the dead flat-margin proximity removed); production DB untouched by the migration test (snapshot copy). **Production DB delta vs v1.23.0**: `schema_migrations` +1 (0036); `stock_holdings` +2 columns (24 rows backfilled). `go build`/`go vet` clean across alert/metrics/store/server; `make build` OK; migration applied + columns verified on `/tmp` DB copy; `/healthz` green post-deploy. Snags carried: S1 (custom-silicon-asic→Optical default), S2 (stale `theses_index.sub_type`). |
| 1.23.0 | 2026-05-31 (night, twelfth pass) | **SC build series — Batch 1 (SC-01 + SC-02 + SC-10).** First batch off the `FT_Build_Handover_SC01-10_Spec9p_2026-05-31.md` consolidated handover (Claude.ai). Three orphaned-data fixes that re-point UI surfaces at the real, now-populated tables. **SC-01 — bottleneck donut is self-maintaining.** The Summary "AI bottleneck chain" donut was hardcoded (`buildStockTagDonut`, now dead/removed); replaced with `buildBottleneckDonut` which derives slices at read-time from `stock_holdings.sector_adapter_subtype` (no `framework_scores` pollution per snag S3). New `subtypeToBottleneckSlice` map keyed on the bare sub-type (part after the `:`, family prefix stripped via `LastIndex(":")`): gpu-accelerator/foundry/semicap-litho→GPU, chip-ip→CPU, edge-silicon→Edge, optical-datacom/custom-silicon-asic/ems-odm→Optical, specialty-chemicals→Chemicals, memory-hbm/advanced-packaging→HBM, diversified-ie→Power, lithium→Battery; quantum-frontier + all non-AI families fall through to Untagged (excluded from the donut, still counted in the M denominator). `custom-silicon-asic` (MRVL) → Optical is a judgment default (snag S1). Render gate relaxed from `coverage ≥ 0.5` to `≥1 tagged stock`; title now "N of M stocks on the AI bottleneck chain"; legend slices carry per-slice ticker lists (hover `title`). **Plumbing gap closed**: `sector_adapter_subtype` was migrated (0034/0035) but never surfaced in Go — added `StockHolding.SectorAdapterSubtype *string`, added the column to `stockSelectCols` + `scanStock` scan (all 3 callers). **SC-02 — Summary nudges re-pointed to real thesis tables.** `updateStaleScoreBanner` rewritten into two collapsible panels: Coverage ("N holdings without a thesis" — `score.source !== 'thesis'`) and Freshness ("N thesis refreshes due — >90d or earnings since update" — `days > 90 || earningsUrgency === 'revision_needed'`). Retired the legacy `thesis_notes` nudge (`updateStaleThesisBanner` kept but no longer called/rendered). Surfaced `earnings_urgency` end-to-end (`ThesisScoreRow.EarningsUrgency` via `COALESCE(earnings_urgency,'none')`; `scoreSummary.earningsUrgency`). **SC-10 — crypto SCORE column overlays locked crypto theses.** New `Store.CryptoThesisScoresBySymbol` joins `crypto_theses` (status='locked', latest per `coin_symbol` via `MAX(id)`) keyed uppercase; `cryptoThesisScoreToSummary` + `pickCryptoScore` wire it into `handleListCrypto` (was wrongly calling the stock-ticker lookup `ThesisScoresByTicker` with crypto symbols → blank). SCORE cell now renders band, e.g. "13/18 — Accumulate" (passes = band ∈ {strong, accumulate}). Stocks unaffected — already overlaid `theses_index` since v1.19B. **Production DB delta vs v1.22.18**: none (read-path + UI only; no migration, no schema change). FT binary rebuilt + redeployed; `go vet`/`go build` clean; `web/app.js` `node --check` OK; `/healthz` green. Snags carried forward: S1 (custom-silicon-asic→Optical default), S2 (stale `theses_index.sub_type` for 4063.T/COHR/PAAS/TSM — not blocking, SC-10 uses score/band not sub-type), S3 (read-time derivation). |
| 1.22.18 | 2026-05-31 (evening, eleventh-pass-B) | **Stock holdings sector_adapter_subtype tagging COMPLETE — all 24 holdings tagged; mechanical master-spec catch-up (snag S7).** Follows v1.22.17 (which applied 6 of 7 re-tags with CLS deferred pending a sub-type decision). The production DB was advanced to target — every `stock_holdings` row now carries a non-null `sector_adapter_subtype` — but the changelog row was still pending; landed here alongside the Batch-1 commit so the history stays linear. **CLS resolved** to `ai-infra-semi:ems-odm` — the new `ems-odm` sub-type anticipated in the v1.22.17 row (hyperscaler-hardware ODM; EMS/ODM split materialised in AI-Infra-Semi v1.2) — closing the deferred 7th re-tag. **Full tag set (24)**: 4063.T `specialty-chemicals`, ABBV `pharma:diversified-immunology`, AEM `mining-metals:gold-tier1`, ALB `mining-metals:lithium`, ARM `chip-ip`, ASML `semicap-litho`, AU `gold-tier1`, CLS `ems-odm`, COHR `optical-datacom`, GLD `asset-hedge:gold-physical`, LLY `pharma:metabolic-obesity`, LSCC `edge-silicon`, MRVL `custom-silicon-asic`, NVDA `gpu-accelerator`, NVO `pharma:glp1-pure-play`, ORCL `cloud-infra:cloud-software-layer`, PAAS `mining-metals:silver-primary`, RGTI `quantum-frontier`, RHM.DE `defense:prime-european-rearm`, SLV `asset-hedge:silver-physical`, SU.PA `industrial-electrical:diversified-ie`, TSM `foundry`, WPM `mining-metals:streaming`, XOM `hydrocarbons:integrated-major` (AI-Infra-Semi family prefix elided in this list). **Production DB delta vs v1.22.17**: 18 additional `stock_holdings` rows updated to non-null `sector_adapter_subtype` (incl. CLS via new `ems-odm`). No schema/migration change. |
| 1.22.17 | 2026-05-31 (early evening, eleventh pass) | **Stock holdings sector_adapter_subtype tagging — Migration 0034 + 0035 + 6 of 7 stock_holdings re-tags applied per Claude.ai's three-answer response to Claude Code's scope questions.** Migration 0034 (`0034_stock_holdings_sector_adapter_subtype.sql`): adds `sector_adapter_subtype TEXT NULL` column + `idx_stock_holdings_sector_adapter_subtype` partial index. Migration 0035 (`0035_relax_sector_adapter_subtype_check.sql`): SQLite table-rebuild relaxing the CHECK constraint from `LIKE '%-%:%'` (which rejected single-word adapter slugs like `pharma:glp1-pure-play`) to `LIKE '%:%'` (just requires colon separator); downstream code is canonical validator; all 47 columns preserved + 2 indexes from 0001 re-created + 1 index from 0034 re-created post-rebuild. Both migrations auto-applied via embed runner on FT restart. **Architecture decisions locked per Claude.ai's response**: Q1 — `sector_universe` retained (load-bearing for Spec 9f rotation tracker + sector_snapshots + heatmap + user_sector_ordering + sector_rotation_digests); Q2 — Option R (Retain both taxonomies as orthogonal axes: "where it rotates" vs "how it's analyzed"; adapter-to-rotation is one-to-many); Q3 — Option B.ii (Migration 0034 acceptable + TEXT column not FK + single primary tag per Defense v1.1 §B5 dominant-segment-by-GP convention; first-class multi-tag is Spec 9i territory). Pattern stored as kebab-case composite `<sector_scorecards.code>:<subtype>`. **6 of 7 stock_holdings re-tags applied**: ORCL → `cloud-infra:cloud-software-layer` (per Cloud-Infra §B Decision 4 infra-revenue dominant); SU.PA → `industrial-electrical:diversified-ie` (per Industrial-Electrical §B Decision 1 dominant ~45% Industrial Automation segment); GLD → `asset-hedge:gold-physical` (per Mining-Metals §B Decision 7 + Asset_Hedge_Adapter_v1.md sub-type table); SLV → `asset-hedge:silver-physical` (same); MRVL → `ai-infra-semi:custom-silicon-asic` (matches theses_index existing sub-type); NVO → `pharma:glp1-pure-play` (matches theses_index existing sub-type). **CLS re-tag DEFERRED pending Claude.ai sub-type decision**: thesis content explicitly states "AI-Infra/Semi (sub-type: hyperscaler hardware ODM — possible future EMS/ODM sub-sector split)" and "If/when EMS/ODM accumulates enough watchlist names (Quanta, Foxconn, Wistron, Jabil etc.) a dedicated adapter may be warranted." None of the 12 existing AI-Infra-Semi v1.1 sub-types (gpu-accelerator/foundry/semicap-litho/semicap-etch-dep/chip-ip/memory-hbm/advanced-packaging/optical-datacom/cooling-thermal/specialty-chemicals/edge-silicon/quantum-frontier) capture CLS's identity. Claude.ai's instinct of `advanced-packaging` doesn't match (CoWoS/fan-out is TSMC/Amkor territory; CLS is system-level integration + EMS). Three options surfaced back to Claude.ai for decision: (i) ship CLS at best-existing-fit `optical-datacom` (acknowledging imperfect — CCS 800G→1.6T is co-packaged optics so it's not crazy); (ii) wait for AI-Infra-Semi v1.2 supplement adding `ems-odm` sub-type then apply; (iii) use placeholder `ai-infra-semi:ems-odm-pending` with v1.2 to follow. **Production DB delta vs v1.22.16**: schema_migrations gains 2 rows (0034 + 0035); stock_holdings table rebuilt (0035) with new column + index; 6 stock_holdings rows updated. Crypto state unchanged (12 theses + 18 cascade rows + 4 history rows). FT binary rebuilt twice (once per migration deploy) + redeployed; `/healthz` green throughout. **Sector_Holdings_Mapping v1.3 unblocked** for Claude.ai authoring — current holdings scope (25 entries); drops BE+MOD (watchlist-only); adds CLS+MRVL+NVO; uses new column format. Sprint window ~9-10 days remaining. Both Claude.ai + Claude Code on standby pending CLS sub-type decision + LINK v1 amendments + first user-authored D25 thesis + v0.5.1 audit (2026-08). |
| 1.22.16 | 2026-05-31 (late afternoon, tenth pass) | **Round-trip follow-on closeouts (v1.22.16) — applied Claude.ai's end-of-session deliverables landed after v1.22.15.** Three small applications: (1) ETH v1 Q2 prose hygiene amendment per `Batch_3d_completion_record.md` §A — Edit applied to `/home/finbarr/Downloads/ETH_v1_locked.md` (contradictory v0.4-style passage at lines 48-54 replaced with v0.5-correct prose; downstream reference at line 257 updated; new doctrinal note appended; verification `grep "Actually wait"` returns 0 in body); DB state unchanged (Q2 = 2 was always stored); LINK v1 amendments still pending per Batch 3d spec §A1.1-A1.7 (user-applied locally). (2) `Asset_Hedge_Adapter_v1.md` canonical replacement — Claude.ai's 14,298-byte richer version (full schema sketch with industrial-demand `weight_by_subtype` matrix + 4 sub-types: `gold-physical`/`silver-physical`/`mixed-physical`/`pgm-physical` + rebalance triggers + cross-adapter calibration section) replaces my interim 8,874-byte draft in both `sector_scorecards.markdown_current WHERE code='asset-hedge'` and `/home/finbarr/Downloads/`; interim file deleted. (3) Energy adapter `current_version` 1 → 1.1 per `Energy_Sector_Adapter_v1_1_Lock_Supplement.md` retroactive lock-resolution pattern; supplement formally closes 5 §7 decisions including SU.PA → Industrial-Electrical cross-resolution, XOM → Hydrocarbons, 6 sub-types retained, scoring math averaged-single-score for current implementation with vector storage deferred to Spec 9i, RR.L Energy-Power leg 11/16 confirmed; §E "no DB UPDATE required (already locked)" — only `current_version` bumped. BTC adapter status flip note acknowledged — no action taken (trivial cosmetic). README.md project-folder navigation aid noted; no DB action. **Production DB delta vs v1.22.15**: `energy-power.current_version` 1 → 1.1; `asset-hedge.markdown_current` 8,874 → 14,298 bytes; sector_scorecards row count unchanged (11/11 locked); crypto state unchanged. **OPEN ITEM — `stock_holdings` re-tag BLOCKED on schema-architecture decision**: Claude.ai's draft `UPDATE stock_holdings SET sector_universe_id = '<adapter>:<subtype>'` won't apply because (i) the column is `INTEGER NULL REFERENCES sector_universe(id)`, not a string; (ii) `sector_universe` uses 34 legacy snake_case codes that don't match the new adapter framework. New `sector_scorecards` (11 rows) and legacy `sector_universe` (34 rows) are parallel taxonomies that aren't 1:1 mapped. Resolution path requires one of three options: (A) add bridging rows to `sector_universe`; (B) Migration 0034 with new `sector_adapter_subtypes` table; (C) use `stock_holdings.sector` TEXT field as kebab-case `adapter:subtype` string. Three scope questions to Claude.ai pending answers before recommendation. Sector_Holdings_Mapping v1.3 deferred until schema decision lands; v1.3 will be scoped to current holdings only (25 entries) when redrafted per Claude.ai's response. **25-vs-24 reconciliation**: mapping v1.2 missing CLS+MRVL+NVO (in stock_holdings) + contains BE+MOD (not in stock_holdings; watchlist-only in theses_index). `Master_Spec_Consolidation_Patch_2026-05-31.md` structural edits deferred — authored against v1.22.10; v1.22.15+v1.22.16 have overtaken most of its content; no operational impact. **Crypto-side D25+D26+D27 COMPLETE. Stock-side sector adapter sweep COMPLETE.** Both Claude.ai and Claude Code on standby pending schema decision + LINK v1 amendments + first user-authored D25 thesis + v0.5.1 audit (2026-08). No urgency. |
| 1.22.15 | 2026-05-31 (afternoon, ninth pass) | **Stock-side sector adapter framework structurally complete — 5 stuck adapters locked at v1.1 + new asset-hedge adapter spawned (10 → 11 sector_scorecards rows; 5 → 11 locked).** Per Claude.ai-authored lock supplements applied 2026-05-31 PM: `Industrial_Electrical_Sector_Adapter_v1_1_Lock_Supplement.md` (resolves 6 decisions incl. SU.PA classification — also retroactively resolves Energy §7 Decision 1; ETN/CARR triggers; 6 sub-types retained), `Cloud_Infra_Sector_Adapter_v1_1_Lock_Supplement.md` (6 decisions: segment-only scoring for mega-cap hyperscalers; neocloud retained with speculative tag + ≤2% position discipline; data-center REITs in-scope ≥50% hyperscaler-revenue; ORCL 13/16; 5 sub-types retained), `Mining_Metals_Sector_Adapter_v1_1_Lock_Supplement.md` (7 decisions: physical ETFs excluded → routed to new asset-hedge adapter; uranium primary scoring under Energy-Power `fuel-cycle`; 9 sub-types retained incl. nickel for future portfolio additions; AEM 12/16; ALB 10/16; streaming sub-type weighting locked), `Pharma_Sector_Adapter_v1_1_Lock_Supplement.md` (5 decisions: pre-commercial biotech excluded with $50M run-rate threshold for inclusion → future Clinical-Stage Biotech adapter to backlog; 5 sub-types retained; LLY/ABBV both 13/16 with different drivers; manufacturing capacity Q1 sub-criterion with sub-type weighting), `Defense_Sector_Adapter_v1_1_Lock_Supplement.md` (6 decisions: 8 sub-types retained; dual-use classification rule LOCKED — trailing 3Q gov-rev ≥50% → Defense; multi-segment scoring convention LOCKED — score dominant segment by GP contribution + secondary segments prose-only; RHM.DE 14/16; RR.L defense leg 12/16 prose-only). **New `asset-hedge` sector adapter spawned per Mining-Metals §B Decision 7** — `Asset_Hedge_Sector_Adapter_v1.md` standalone authored (~8.8KB) with simplified 4-pillar /8 scorecard (A1 Monetary Regime + A2 Intensity/Macro Flow + A3 Sovereignty/Jurisdiction + A4 Technicals) and pass threshold 5+/8 with A1 ≥ 1; distinct from Mining-Metals because storage products lack operational drivers (no AISC, no reserve life, no mine economics) — same "right framework for the right asset" discipline that gives BTC its own `monetary_12` /12 scorecard. Asset Hedge worked examples: GLD 7/8 (Strong-pass) + SLV 4/8 (Below pass-threshold, hold-only). INSERT row in `sector_scorecards` with code='asset-hedge' + current_version='1.0' + display_name='Asset Hedge — Physical Metal ETFs' + status='locked'. **Cross-adapter calibration distribution validated coherent** (all 8 stock adapters now producing harmonized scores across portfolio): 14/16 RHM.DE+ASML / 13/16 LLY+ABBV+ORCL / 12/16 AEM+NVDA+SU.PA+RR.L-defense-leg / 11/16 RR.L-Energy-Power+XOM / 10/16 ALB / 7/8 GLD / 4/8 SLV. **First time the v0.6.1 §D strict patch immutability discipline has been applied outside the crypto framework** — original sector adapter MD bodies unchanged in `markdown_current`; lock supplements are standalone artifacts. Cross-domain pattern now demonstrated, not just theorized. Each adapter status flipped via single atomic UPDATE (code-column-name correction vs Claude.ai's draft SQL: `code` not `adapter_slug`; `updated_at` not `last_updated_at`; `current_version` bumped 1 → 1.1 simultaneously). Master-spec also bumped `current_version` 1.22.6 → 1.22.15 to reflect cumulative work. **Followups carried forward for Claude.ai** (small, parallel-doable): (1) Energy_Sector_Adapter_v1_1_Lock_Supplement.md to formally close out Energy §7 Decision 1 (cross-resolved by Industrial-Electrical §G — separate ~10 min Claude.ai supplement maintains the patch-immutability discipline); (2) Sector_Holdings_Mapping_v1.2 with SU.PA re-tag and GLD/SLV asset-hedge mapping (~10 min Claude.ai); (3) future adapter backlog additions per supplements (Robotics & Embodiment, Aerospace, Software/SaaS, REIT, Clinical-Stage Biotech); (4) Mining_Metals §F recommended `Asset_Hedge_Adapter_v1.md` standalone authoring — DONE in this batch (~8.8KB in `/home/finbarr/Downloads/`). **No Go code changes this batch — pure DB + spec work.** FT binary unchanged from v1.22.14. Production state cumulative: 12 crypto theses + 18 cascade rows + 4 history rows + 11 sector_scorecards rows all locked + 39 stock theses all locked. **Stock-side sector adapter sweep + D25 + D26 + D27 all COMPLETE.** Claude Code standby resumed; sprint window ~10 days remaining for v0.5.1 audit cadence (2026-08) + first user-authored crypto thesis via D25 form (UX validation) + any future feature work. |
| 1.22.14 | 2026-05-31 (afternoon, ninth-pass-A) | **D27 fork-to-v2 workflow shipped — closes v0.6.1 §B post-D26 backlog.** Closes the last architectural item flagged in Claude.ai's v0.6.1 §B supplement ("D26 follow-on: Fork-to-v2 workflow — when acknowledgment alone doesn't suffice — substantive thesis content changes spawn v2"). **Backend**: new `internal/cryptotheses/fork_to_v2.go` (~210 lines) with `ForkToV2(ctx, symbol, version, note) (*ForkResult, error)` service method on `ThesisWriteService`. New errors: `ErrCannotForkDraft`, `ErrCannotForkForked`, `ErrCannotForkInvalid`, `ErrVersionUnparseable`; `ErrVersionExists` reused. Workflow (atomic tx): validate source exists + status ∈ {locked, needs-review} → compute next version via `versionRE` regex (`^v(\d+)$` → vN+1) → check vN+1 doesn't exist → auto-resolve unresolved `cascade_events` on source (note: "source X vN forked to vN+1; events auto-resolved") → INSERT new draft at vN+1 inheriting ALL editable content from vN (coin_symbol/name/coingecko_id/adapter_ids/scorecard/pillar_scores_json compound shape preserved/total_score/max_score/band/ppg_flag/q5_mechanism/q5_quant/q9_team_note/catalyst/horizon/btc_beta/secondary_tags_json including `__meta__:` prefix tags/liquidity fields/DePIN RYR fields/RWA RABR + custody fields/markdown_current + rendered_html) → INSERT `fork_rescore` history row on SOURCE (`event_reason='fork_source: vN spawned vN+1 — <note>'` + `recommended_action='override'`) → INSERT `fork_rescore` history row on TARGET (`event_reason='fork_target: vN+1 spawned from vN — <note>'` + `recommended_action='none'`) — reuses existing `fork_rescore` event_type from Migration 0031 CHECK enum (**no Migration 0034 needed**) → UPDATE source SET status='forked' (already in 0031 CHECK enum). Cascade dependency rows NOT copied — they re-create when v2 locks via existing D25 lock auto-cascade path; source vN's cascade rows remain pointing at vN (now forked), preserving audit lineage. **Endpoint**: `POST /api/crypto/theses/{symbol}/{version}/fork` (cookie auth via `requireUser`); optional body `{"note": "..."}`. Handler `handleCryptoThesisFork` + `mapForkErr` (404 ErrThesisNotFound; 400 ErrCannotForkDraft/Forked/Invalid + ErrVersionExists + ErrVersionUnparseable). Route wired in `server.go` between `/acknowledge-cascade` and DELETE. **Frontend**: per-thesis detail modal now surfaces purple-accent `.d27-fork-action` banner with `⑂ Fork to vN+1…` button when `status='locked'`; D26 `🔄 NEEDS REVIEW` banner extended with parallel `Fork to vN+1…` button alongside `Review cascade events + re-lock…` — gives author both paths from same surface. Click → `d27OpenForkFlow(thesis, parentModal)` opens confirmation modal explaining workflow (5-bullet list covering content inheritance + audit trail + cascade row re-creation on v2 lock) + optional rationale note input + Fork/Cancel buttons. Success path: POST roundtrip → success alert summarizing source X vN (previousStatus → forked) → new X vN+1 (draft) + `cascadeEventsResolvedCount` + both history row ids → both modals close → `state.cryptoView = 'edit:SYM:VN+1'` → `renderCryptoEditDraftPage()` drops user directly into the new draft for editing. New CSS: `.d27-fork-action` purple/violet accent (rgba 168,85,247,0.08) — distinct from D26 indigo. **5 D27 ACs PASSED** via new `cmd/ft-d27-test/`: AC1 Fork from locked → v2 draft + v1 forked; AC2 Fork from needs-review → v2 draft (source previousStatus carryover verified); AC3 Fork from draft → ErrCannotForkDraft (no-op forbidden); AC4 Sub-criteria + adapter-specific quant fields (q5_rabr/q6_custody_tier/q5_auditor/q5_mechanism) + compound pillar_scores_json shape all copied to v2 verbatim; AC5 Cascade events auto-resolved (count=1 verified) + history rows on both source (`fork_source:%`) and target (`fork_target:%`) written. Test infrastructure inventory grows: 60+ test cases across 8 artifacts (5 added this batch). Production state unchanged (12 theses + 18 cascade rows; cascade_events 0; history 4). No DB writes; nothing forked in production. FT binary rebuilt via `make build`, deployed `/opt/ft/bin/ft`, service restarted clean, `/healthz` green, served `app.js` confirms 8 occurrences of `d27` + `/fork`. **D25 + D26 + D27 = full crypto feature group COMPLETE.** |
| 1.22.13 | 2026-05-31 (mid-morning, eighth-pass-B) | **D25 Phase 2 polish — inline lock error rendering (P2.AC9 closed) + BTC monetary_12 sub-criteria extraction + latent M1-M6 vs P1-P6 backend bug fix.** **Polish A** (inline error rendering, closes P2.AC9 from v0.9.1 §E "9.5/10 satisfied" → now 10/10): `d25LockDraft` now catches lock errors and surfaces them inline rather than via `alert()`. Three error paths handled: (1) `missing mandatory field "<field>"` → parse field name via regex, look up in new `D25_FIELD_TO_INPUT_ID` map (q4q5Ryr → d25-ryr, q5PaidRevenueUSD → d25-paid-rev, q5VerifiedAssetValueUSD → d25-vav, q6CustodyTier → d25-custody-tier, etc.) → surface error message as red text below the input + add `.has-error` class to wrapping `.d25-field` + auto-scroll into view; (2) `pillar QN sub-criteria required for lock` → parse pillar key, add `.has-error` class to the pillar block + show explanatory error message inside the block; (3) fallback top-of-form red `.d25-lock-error-banner` for unmappable errors (removed on next attempt). New helpers `d25ClearAllFieldErrors` + `d25ShowFieldError` + `d25SurfaceLockError`. Symbol/Name validation now also inline instead of `alert()`. **Polish B** (BTC monetary_12 sub-criteria extraction): `tools/extract_adapter_schemas.py` extended with `BTC_PILLARS=M1..M6` + `BTC_PILLAR_LABELS` dict + prefix-parameterized `split_into_pillar_sections(md, prefix='Q')` + `extract_pillar_label_for_prefix(section, prefix)` helper. Main loop adds `out["btc"]` with M1-M6 schema extracted from BTC adapter MD §3 (Six-Pillar Monetary-Asset Scorecard) — canonical sub-criteria captured: M1 Cycle Phase (4 subs: 200WMA position / Cowen log regression band / Cowen Risk Indicator 0-1 composite), M2 Macro Regime (4 subs: DXY trend / US 2Y yield trend / ISM Manufacturing PMI / Global M2 ROC when 9e Phase 2 ships), M3 Flows (3 subs: ETF 7-day net flows / stablecoin supply trend 30d / sovereign+corporate treasury announcements 90d), M4 Network Health (4 subs: hash rate 90d trend / active addresses 90d MA / MVRV-Z score / miner balance + sell pressure), M5 Sentiment (3 subs: Crypto F&G / funding rates when 9e Phase 2 ships / Google Trends "bitcoin" 90d relative), M6 Technical & Structural Regime (4 subs: price vs 200WMA / weekly RSI / cycle structure higher highs+higher lows / wrapper+custodian quality sub-type-dependent). Spliced into `web/app.js` `D25_SCHEMA_BY_ADAPTER.btc`. **Latent backend bug fixed: M1-M6 vs P1-P6** — `ValidateForLock` at `internal/cryptotheses/thesis_write_service.go:269` expected `monetary_12` keys to be `{"P1".."P6"}`. Canonical convention per BTC adapter MD §3 is M1-M6 (and BTC v1 fixture stores `pillar_scores_json` as `{"M1":..,"M6":..}`). Bug never triggered because no `monetary_12` thesis was ever locked via D25 (BTC v1 was a seed fixture, not D25-authored) — discovered while extending the schema extractor for polish B. Fixed: `expectedKeys = {"M1".."M6"}` for `monetary_12`; comment added explaining canonical convention + history. Updated `D25_SCHEMA` generic `monetary_12` fallback in `web/app.js` from P1-P6 to M1-M6 to stay consistent with backend. New CSS (~32 lines) for inline error styling: `.d25-field-error`, `.d25-field.has-error`, `.d25-pillar-block.has-error`, `.d25-lock-error-banner`. All existing harnesses still PASS (no regression on alt_18 ACs / chained scenarios / acknowledgment ACs / scoring drift): cmd/ft-d25-p1-test 11/11; cmd/ft-d25-p3-test 6/6; cmd/ft-d26-test 3/3; `internal/web/assets-test/d25_js_scoring_test.js` 15/15. Production DB unchanged (12 theses + 18 cascade rows; cascade_events 0; history 4). No DB writes this batch. FT binary rebuilt via `make build`, deployed `/opt/ft/bin/ft`, service restarted clean, `/healthz` green. |
| 1.22.12 | 2026-05-31 (mid-morning, eighth-pass-A) | **D25 Phase 2 polish — `D25_SCHEMA` per-adapter sub-criterion labels (closes Phase 2 architectural Q A TODO).** Form sub-criterion labels now come from canonical adapter MD prose instead of generic "Sub-criterion 1" placeholders. New `tools/extract_adapter_schemas.py` extracts per-adapter, per-pillar sub-criterion labels from `sector_scorecards`-style adapter MDs in the live DB by splitting MD on `### Qn` headings → finding first markdown table with "Sub-criterion" in header → extracting first-column values from data rows → stripping `**bold**` + `*italic*` markdown → emitting JS const `D25_SCHEMA_BY_ADAPTER` mapping slug → `{Q1..Q9: {label, subs}}`. Output spliced into `web/app.js` as new const right after the existing generic `D25_SCHEMA`. Covers all 7 alt_18 adapters: l1, l2, defi, infra, depin, rwa, speculative. monetary_12 (BTC only) still uses generic labels at this point — fixed in v1.22.13 polish. New helper `d25PillarsForAdapter(adapter)`: if `D25_SCHEMA_BY_ADAPTER` has the adapter slug, use that; else fall back to generic `D25_SCHEMA[scorecardType]`. Three form code-paths updated to use the helper: `d25RenderForm` + `d25RecomputeAndRender` + `d25RenderSummary` + sub-criteria initialization on first radio click. Sub-criterion counts now differ per adapter (e.g., L1 Q1 = 3 subs vs generic 4; L2 Q1 = 4 subs incl. "Position within L2 narrative rotation"; DeFi Q1 = 4 subs incl. "Position within DeFi sub-sector rotation"; L1 Q2 = 5 subs incl. "Supply cap structure"; L1 Q3 = 4 subs vs generic 5; L1 Q9 = 5 subs vs generic 6). Inline math annotation now correctly reflects per-adapter sub-criterion shape. Existing `subCriteria` reset on adapter switch (existing handler) so new schema picks up cleanly without stale carry-over. JS syntax verified via Node `Function()` check. Served bundle confirmed to contain 32 `d25PillarsForAdapter` + `D25_SCHEMA_BY_ADAPTER` references. Production state unchanged (12 theses + 18 cascade rows). No backend Go changes — pure FE polish. |
| 1.22.11 | 2026-05-31 (morning, eighth pass) | **Spec 9l v0.9.3 supplement (Claude Code as-built) — D26 needs-review resolution UI shipped + AAVE/BUIDL acknowledged in production.** Closes the v0.6.1 §B backlog item per Claude.ai's strict patch immutability supplement (authored 2026-05-30 evening): "D26 — needs-review resolution UI. Estimated 1 session. New endpoint POST /api/crypto/theses/{symbol}/{version}/acknowledge-cascade transitions needs-review → locked after author confirmation. UI button in locked thesis detail view 'Acknowledge cascade impact → re-lock' (only visible when status=needs-review). Confirmation modal showing the triggering cascade event(s) from cascade_events audit log. Audit trail: append cascade_acknowledgment event to log with timestamp + acknowledger. Validators: pillar scores must still pass PPG; if cascade has materially changed thesis viability, author should fork to v2 instead." All listed scope items shipped. **Backend**: new `internal/cryptotheses/cascade_acknowledge.go` with `AcknowledgeCascade(ctx, symbol, version, note) (*AcknowledgeResult, error)` service method on `ThesisWriteService` + new errors `ErrNotNeedsReview` + `ErrPPGNowFails`. `AcknowledgeResult` shape: `{thesisId, symbol, version, previousStatus, newStatus, cascadeEventsResolvedCount, historyRowId}`. **Workflow** (atomic tx): validate `status='needs-review'` (other states reject with 400) → parse stored `pillar_scores_json` via `ParsePillarScoresJSON` (dual-mode reader handles both legacy + compound shapes) → re-verify PPG using authoritative `ComputeRawAndFinalBand` (defensive guard against data drift between flagging and acknowledgment — if PPG now fails AND recomputed final band is worse than stored, reject with `ErrPPGNowFails` so author forks to v2 instead) → `UPDATE cascade_events SET resolved_at = NOW, resolution_note = caller_note WHERE affected_thesis_id = id AND resolved_at IS NULL` (returns events_resolved count; gracefully handles zero rows for historical needs-review with cleaned-up cascade_events) → `INSERT INTO crypto_thesis_history` with `event_type='event_rescore'` + `event_reason='cascade_acknowledgment: <note>'` + `triggered_by='user'` (reuses LINK v0.5 §L.9.4 pattern — **avoids Migration 0034 escalate** since CHECK enum already permits `event_rescore`) → `UPDATE crypto_theses SET status='locked', last_reviewed_at = NOW WHERE id = ? AND status='needs-review'`. **Endpoint**: `POST /api/crypto/theses/{symbol}/{version}/acknowledge-cascade` (cookie auth via `requireUser`); optional body `{note: "..."}`; new handler `handleCryptoThesisAcknowledgeCascade` + `mapAcknowledgeErr` (404 for `ErrThesisNotFound`; 400 for `ErrNotNeedsReview`, `ErrPPGNowFails`, default). New `decodeJSONOptional` helper for optional-body endpoints. Route registered in `internal/server/server.go` between `/lock` and `/{symbol}/{version}` DELETE. **Frontend**: per-thesis detail modal in `openCryptoThesisDetail` now surfaces `🔄 NEEDS REVIEW` indigo-accent banner (new `.d26-needsreview-box` CSS class) with `Review cascade events + re-lock…` button when `status='needs-review'`. Click → `d26OpenAcknowledgeFlow(thesis, parentModal)` fetches unresolved cascade events via existing `GET /events?unresolvedOnly=true` → renders confirmation modal listing each event (`dependencyType` + `priority` + `triggerReason` + `action` + UTC timestamp via `.d26-event-list` mono-spaced styling; empty-state message when no underlying events for historical needs-review) + optional acknowledgment note input + Acknowledge/Cancel buttons. POST roundtrip displays success alert summarizing `previousStatus → newStatus`, `cascadeEventsResolvedCount`, `historyRowId`; both modals close; `renderCryptoTheses()` re-renders the Crypto Theses tab so updated status reflects. **3 D26 ACs PASSED** via new `cmd/ft-d26-test/`: D26.AC1 happy path — seed `CHILDACK` at `needs-review` + seed `cascade_events` row with `flagged_needs_review` action → `AcknowledgeCascade('CHILDACK','v1','test ack note')` → verify status=locked + cascade event `resolved_at` populated + `resolution_note='test ack note'` + history row with `event_type='event_rescore'` and `event_reason LIKE 'cascade_acknowledgment:%'`. D26.AC2 reject when `status='locked'` (`LOCKEDFIX` test fixture) → `ErrNotNeedsReview`. D26.AC3 reject when `status='draft'` (`DRAFTFIX` test fixture) → `ErrNotNeedsReview`. **Production resolution applied** via new `cmd/ft-d26-resolve-prod/` one-off (parameterized by `-db` flag): AAVE (id=10) + BUIDL (id=12) both held `needs-review` since Batch 3a + EIGEN closeout cascade firing tests (where their `cascade_events` rows were cleaned up post-test but status persisted). Both acknowledged with note "D26 launch — historical needs-review from Batch 3a + EIGEN closeout cascade tests; pillar scores unchanged; re-locked for clean state going forward" → both now `status='locked'`, 2 new `event_rescore` history rows captured (rows 3 + 4; events_resolved=0 in both cases because cascade_events were already cleaned). **All 12 production theses now `locked`; zero `needs-review` lingering. Production state ready for next cascade firing cycle. FT binary rebuilt via `make build`, deployed `/opt/ft/bin/ft`, service restarted clean, `/healthz` green, `curl http://127.0.0.1:8081/app.js` confirms 11 occurrences of `d26` + `acknowledge-cascade` in served bundle. Test infrastructure inventory grows: `cmd/ft-d25-p1-test/` 11 ACs + `cmd/ft-d25-p3-test/` 6 scenarios + `cmd/ft-d26-test/` 3 ACs + `cmd/ft-d26-resolve-prod/` 1 production one-off + 15-fixture JS drift test + 14 Go unit fixtures + 7 cascade firing cases = **57+ distinct test cases across 7 artifacts** (4 added this batch). **D25 + D26 = COMPLETE feature group.** Sprint state: ~10 days remaining for Batch 3d MD prose amendments (Claude.ai authors) + any user-driven thesis authoring via D25 form + Claude.ai-side STATE_v3 + BACKLOG_v3 cleanup + buffer. `FT_Spec_9l_v0_9_3_supplement.md` (Claude Code as-built, D26) authored per v0.6.1 §D strict patch immutability — supplements v0.9 + v0.9.1 + v0.9.2 + v0.6.1; supersedes nothing. Production DB delta vs pre-D26: 2 history rows added (3 total now); status: 12 locked (was 10 locked + 2 needs-review); cascade_events still 0; cascade rows still 18; theses still 12. |
| 1.22.10 | 2026-05-30 (evening, seventh pass) | **Spec 9l v0.9.2 supplement (Claude Code as-built) — D25 Phase 3 integration COMPLETE; D25 ship-gate fully green.** New `cmd/ft-d25-p3-test/main.go` chained integration harness (~340 lines) exercises 6 scenarios as multi-step flows against ephemeral SQLite (`/tmp/ft-d25-p3-test.db`, non-destructive to production). All 6 PASSED: **(A)** Standard DeFi creation — Create draft `P3DEFI` (DeFi/multi_year/full subs) → Re-edit (horizon→medium + Q1 sub-criteria→`[1,2,2,1]` to trigger tie-break) → intermediate Get confirms Q1=1 via v0.5.1 #4 round-down → Lock → total=13 raw=strong final=strong + cascade rows `ETH→P3DEFI [protocol_host, moderate]` + `BTC→P3DEFI [btc_beta_implicit, weak]` → DB verify status=locked + 2 cascade rows in `crypto_thesis_dependencies`. **(B)** DePIN mandatory RYR — Create `P3DEPIN` with NULL Q4Q5RYR/paid_revenue/emissions/age → Lock rejected with `missing mandatory field "q4q5Ryr"` → confirm status remains `draft` (**lock atomicity invariant verified — failed lock does not partially mutate state**) → populate RYR (0.35, $1.4M, $4M, 30mo) → re-lock succeeds (total=14 band=strong). **(C)** RWA mandatory RABR + Custody — Create `P3RWA` with full RABR + `tier_3` custody → Lock succeeds despite Tier 3 (Tier 3 = soft warning per RWA adapter §3, NOT hard block) → DB verify `q6_custody_tier='tier_3'` persisted. **(D)** VETO triggered — Create `P3VETO` with ALL pillar sub-criteria = `2` (would otherwise be 18/18 Strong) + `FounderRug=true` → Lock → `VetoTriggered=true`, `finalBand=exit`, DB `active_veto='founder_rug'` (**VETO override beats both pillar math AND PPG cap per Spec 9l §20**). **(E)** PPG cap — EIGEN-like Q8=0 (sub-criteria `[0,0,1,1,0,1,1,1]`) with other pillars at 2 → total=14 raw=strong (13+) → PPG fails on `Q8_pillar_zero` → final=accumulate (one below) — `PPGFailedGates` list surfaces explicit failure reason for audit. **(F)** End-to-end cascade firing — Create + Lock `P3CASC` (DeFi + `OracleDependencyParentSymbol=LINK`) → forward `oracle_dependency` row created in `crypto_thesis_dependencies` → simulate LINK band drop Strong→Hold (2-band drop satisfies `oracle_dependency` moderate trigger) → `CheckCascadeOnRescore` fires 1 event flagging P3CASC → DB confirms `P3CASC.status='needs-review'` (**full chain from declaration → row creation → parent band change → child flagging verified end-to-end**). **Test infrastructure inventory after D25** (5 artifacts, 53+ distinct test cases): `cmd/ft-d25-p1-test/` 11 atomic ACs at service layer; `cmd/ft-d25-p3-test/` 6 chained scenarios; `internal/web/assets-test/d25_js_scoring_test.js` 15-fixture Node-runnable JS↔Go drift test; `internal/cryptotheses/scoring_test.go` 14 Go unit fixtures; prior `cmd/ft-cascade-firing/` + `cmd/ft-cascade-firing-eigen/` 7 firing acceptance cases. All non-destructive; none touch `/var/lib/ft/ft.db`. **D25 ship-gate**: Phase 1 + Phase 2 + Phase 3 = COMPLETE. **D25 actual cost**: ~8 hours total (Phase 1 ~4h, Phase 2 ~3h, Phase 3 ~1h) vs **4-6 days estimated** in build doctrine — significant under-estimate margin due to (1) v0.6.1 §A resolving v0.5.1 #4 pre-Phase-1 (saved round-trip), (2) Phase 1 backend was mechanical doctrine→Go translation, (3) Phase 2 reused FT SPA patterns (no new infra), (4) Phase 3 reused Phase 1 service constructors + existing cascade engine. **D26 unblocked** — needs-review resolution UI per v0.6.1 §B; estimated 1 session; AAVE + BUIDL currently in `needs-review` state in production await D26 to resolve. Production DB unchanged throughout D25 Phase 1+2+3 work (12 theses + 18 cascade rows; cascade_events still 0; history still 2 rows from LINK v0.5 re-lock). FT binary unchanged from v1.22.9 (this batch is test-harness-only — no Go regressions in production path). `FT_Spec_9l_v0_9_2_supplement.md` (Claude Code as-built, Phase 3) authored per v0.6.1 §D strict patch immutability — supplements v0.9 + v0.9.1 + v0.6.1; supersedes nothing. **Sprint state**: ~10 days remaining for D26 + Batch 3d cleanup + any user-driven thesis authoring + buffer. Claude Code standby resumed. |
| 1.22.9 | 2026-05-30 (evening, sixth pass) | **Spec 9l v0.9.1 supplement (Claude Code as-built) — D25 Scoring Engine Phase 2 frontend shipped.** Three new pages added to Crypto Theses tab via `state.cryptoView` dispatch (no router refactor): `list` (default — existing cross-thesis table + new `+ Create new thesis` + `📝 Draft theses…` buttons), `drafts` (simple table with edit/delete per draft + back button), `create` (full sectioned form), `edit:SYM:VER` (same form pre-populated from existing draft via `d25ThesisToFormState`). **Form structure per build doctrine §"Phase 2 frontend"**: (1) Identity section (Symbol uppercase + Version + Name + CoinGecko ID + Adapter dropdown + Sub-type free-text + Primary chain symbol for protocol_host cascade + Horizon dropdown + BTC-β dropdown + optional secondary adapter); (2) Pillar scoring section — 9 expandable pillar blocks for `alt_18` scorecard (Q1-Q9) or 6 for `monetary_12` (P1-P6), each with sub-criteria as 0/1/2 segmented radio controls + **inline rounding math annotation per Decision 3** rendered live (e.g., *"Sub-criteria [1,2,2,1] → avg 1.50 → exact tie (avg 1.5), v0.5.1 #4 round DOWN → pillar = 1"*) — math display updates on every keystroke; (3) Q5 mechanism + quantification (14-value dropdown sourced from Migration 0033 enum + annualized $ + FDV + free-text mechanism note); (4) Adapter-specific mandatory fields — conditionally rendered: `depin` shows RYR fields panel (q4q5Ryr + paid revenue + emissions + network age — explicit dim note that pre-revenue protocols enter `0` not blank), `rwa` shows RABR fields panel (q5Rabr + verified asset value + token supply at par + audit date + auditor) + Custody Verification Tier panel (tier_1/2/3 dropdown + cadence + jurisdiction — surfaces ⚠ tier_3 soft warning per RWA adapter §3 when selected); (5) Forward cascade declaration — DeFi+Speculative only, dropdown of locked Infrastructure theses with `{symbol} v{version} — {name} ({band})` labels per architectural Q E option B; (6) VETO conditions checklist — 6 universal conditions (unlock cliff / SEC enforcement / holder concentration / exploit unresolved / founder rug / liquidity prefilter) each with checkbox + conditional note input that enables when checked; (7) Q9 team note + catalyst + liquidity venues comma-list + markdown body + next review date. **Live computed summary panel** (sticky right side, 320px column): raw total/raw band/PPG status (PASS green / FAIL red with failed gate names)/VETO status (clean green / 🛑 triggered red)/final band (with PPG cap or VETO override note). VETO triggered surfaces prominent banner *"🛑 VETO will trigger — band forced to Exit on lock regardless of pillar math."* Computation done client-side via `d25ComputeRawAndFinalBand` for instant feedback; server is still authoritative on lock per architectural Q B option C. **Action buttons** (header): "save draft" (POST/PUT and redirect to drafts list with success alert), "lock thesis" (saves first then POSTs /lock then shows summary alert with cascade rows created + final band + VETO/PPG notes + redirects to thesis list), "delete" (DELETE with confirm — edit mode only), "← Back". **JS port `d25ComputePillarScore`** is byte-identical to Go `ComputePillarScore` (v0.5 strict + v0.5.1 #4 tie-break LOCKED — round DOWN on exact 0.5/1.5 ties). **JS↔Go drift-prevention test**: new `internal/web/assets-test/d25_js_scoring_test.js` Node-runnable script — 15 fixtures: 10 pillar-level (AAVE Q1 tie [1,2,2,1]→1; EIGEN Q1 same; EIGEN Q2 [0,0,1,0,1]→0; EIGEN Q8 [0,0,1,1,2,0,1,1]→0; LINK Q6 [2,1,2,2,2,2]→2; 5+ pillar 2 lows [1,2,1,2,2]→1; clean round 1.75 [2,2,2,1]→2; all-1s [1,1,1,1]→1; empty []→0; all-2s clamp [2,2,2,2,2]→2) + 5 band-level (EIGEN triple-zero cap hold→trim; AAVE Q8=0 cap strong→accumulate; LINK clean strong; BUIDL clean strong; LUNC exit floor) — **15/15 PASSED**. **Sub-criteria labels schema** (`D25_SCHEMA` const in app.js) hard-coded per architectural Q A option B — Phase 3 will extract from adapter MDs (TODO comment in source). 9 pillars × 4-8 sub-criteria each for `alt_18`; 6 generic pillars for `monetary_12` (BTC scorecard) since BTC sub-criteria MD content wasn't extracted in time. **Q5 mechanism dropdown** surfaces all 14 enum values from Migration 0033 with prose labels (e.g., "real_yield_staking (paid from real fees)", "governance_with_fee_switch (proposed not active)"). **New CSS section** in `web/app.css` (~265 lines): `d25-*` class prefix; sticky summary pane (top:1rem); segmented sub-criterion controls (radio + label trick); grid-2 form layout; inline math line muted gray; VETO checkbox row with disabled-note pattern; tier_3 soft warning banner; mobile breakpoint at 1024px collapses summary pane to static. **New `.btn-primary`** class added (was missing from CSS — existing buttons were all `.btn-ghost`). **`state.cryptoView`** added to global state object (defaults to `'list'`). **Backend Phase 1 unchanged** (10/10 ACs from v1.22.8 still pass; no Go code changes this batch). **Production DB unchanged** (12 theses + 18 cascade rows). FT binary rebuilt via `make build` (which runs `sync-assets` to copy `web/` → `internal/web/assets/` before `go build`), deployed `/opt/ft/bin/ft`, service restarted clean, `/healthz` confirms green, `curl http://127.0.0.1:8081/app.js` confirms 8 occurrences of `d25Compute` + 23 occurrences of D25 form/render helpers present in served bundle (cache-bust version hash regenerated automatically). **P2 acceptance verification** (pragmatic substitutes for full browser E2E which would require Playwright): (1) JS↔Go drift test 15/15 PASS; (2) Phase 1 backend ACs cover all lock/validation paths (already 11/11 from v1.22.8); (3) JS syntax valid (SPA boots clean per `/healthz`); (4) form structure code-path verified via grep. Full browser-clicking E2E deferred to Phase 3 integration scenarios per build doctrine (those are end-to-end + better exercise the click→form→save→lock→cascade path). **D25 ship-gate now: Phase 1 + Phase 2 backend+frontend complete; Phase 3 end-to-end integration remains.** `FT_Spec_9l_v0_9_1_supplement.md` (Claude Code as-built, Phase 2) authored separately per v0.6.1 §D strict patch immutability — supplements v0.9 + v0.6.1; supersedes nothing. |
| 1.22.8 | 2026-05-30 (evening, fifth pass) | **Spec 9l v0.9 patch (Claude Code as-built) — D25 Scoring Engine Phase 1 backend shipped.** Per `D25_Build_Doctrine_Handoff.md` 6 locked scope decisions + `FT_Spec_9l_v0_6_1_supplement.md` doctrinal resolutions (v0.5.1 #4 tie-break LOCKED retroactively + needs-review carve-out + strict patch immutability + lesson #4). 5 new endpoints registered in `internal/server/server.go`: `POST /api/crypto/theses` (create draft), `PUT /api/crypto/theses/{symbol}/{version}` (update draft; 400 on `locked` or `needs-review` per v0.6.1 §B), `POST /api/crypto/theses/{symbol}/{version}/lock` (transition draft → locked with cascade auto-creation + VETO override + PPG cap), `GET /api/crypto/theses/drafts` (list), `DELETE /api/crypto/theses/{symbol}/{version}` (draft only). All require `requireUser` (cookie auth); existing read endpoints (`/api/crypto/theses` list + per-thesis detail + events) retain `requireUserOrToken`. **New `internal/cryptotheses/scoring.go`** with `ComputePillarScore(SubCriteria) int` (v0.5 strict — round DOWN if any sub-crit=0, round DOWN if 2+ sub-crit ≤1 in 5+ pillar — **plus v0.5.1 #4 tie-break LOCKED per v0.6.1 §A: round DOWN on exact 0.5/1.5 ties**, retroactively applied to AAVE Q1 + EIGEN Q1 fixtures [1,2,2,1] → 1) + `ComputeAllPillars` + `ComputeTotal` + `ComputeRawAndFinalBand` (PPG cap single-band-below per Spec 9l §15, NEVER stacked — multi-zero produces ONE cap not 3) + `ApplyVeto` (overrides pillar math + PPG, forces band=Exit per Spec 9l §20) + `ParsePillarScoresJSON` dual-mode reader (legacy `{"Q1":1,...}` + compound `{"Q1":{"subs":[...],"score":N},...}`) + `ParseSubCriteriaJSON` + `FilterMetaTags`. **New `internal/cryptotheses/thesis_write_service.go`** with `DraftThesisInput` shape (identity + sub-criteria map + Q5 mechanism + DePIN/RWA optional fields + cascade declarations + VETO conditions + body + next review) + `VetoConditions` struct (universal 6: unlock_cliff/sec_action/holder_concentration/exploit/founder_rug/liquidity_prefilter + adapter-specific map + per-condition notes) + `validateShape` (pre-lock shape check including Speculative horizon trigger) + `ValidateForLock` (hard adapter-specific enforcement per Decision 4 — DePIN requires `q4q5Ryr`+`q5PaidRevenueUSD`+`q5EmissionsUSD`+`networkAgeMonths`; RWA requires `q5Rabr`+`q5VerifiedAssetValueUSD`+`q5TokenSupplyAtParUSD`+`q5AuditDate`+`q5Auditor`+`q6CustodyTier` — pre-revenue DePIN edge case accepts `q5PaidRevenueUSD=0.0` as meaningful, NULL rejected) + `CreateDraft`/`UpdateDraft`/`DeleteDraft`/`ListDrafts`/`Lock` methods. **Lock transaction** opens tx, recomputes scoring authoritatively, applies VETO override, persists with `pillar_pass_gate_failed` + `active_veto`/`active_veto_reason`/`veto_tripped_at` columns, then auto-creates cascade rows: (1) upward `protocol_host` moderate from `primary_chain_symbol` field (lookup by symbol + v1 + locked|needs-review status); (2) upward `btc_beta_implicit` weak from `btc_beta` field, skipping when value=`reference` (self-cascade prevention); (3) forward `oracle_dependency` moderate from `oracle_dependency_parent_symbol` field per Decision 5 — validators: parent must exist locked, parent adapter must be Infrastructure (ErrNonInfraOracleParent), self-reference rejected (ErrSelfDependency), duplicate row rejected (ErrDuplicateDependency); (4) `initial_lock` history row with snapshot. **Transient lock-time fields persisted as `__meta__:` prefix tags** in `secondary_tags_json` — `primary_chain=X`, `oracle_dep_parent=X:vN`, `veto=slug`, `veto_note=slug:text`, `veto_adapter=key`, `veto_adapter_note=key:text` — encoded by `encodeMetaTags(in)` at write, decoded by `decodeMetaTags(tags, &in)` at re-load before lock, **stripped from public API by `FilterMetaTags` in `scanThesisRow`**. Avoids Migration 0034 escalate per v0.6.1 §D strict patch immutability + handoff doctrine NEGOTIABLE bucket. **Compound `pillar_scores_json` shape** `{"Q1":{"subs":[1,2,2,1],"score":1},"Q2":{...},...}` per Decision 1 ("store both" sub-criteria AND computed pillar score) — `buildPillarScoresJSON` emits compound for D25-authored; legacy `{"Q1":1,...}` preserved for 12 seed fixtures via dual-mode reader. Sub-criteria for seed fixtures remain unavailable (their MDs in `/home/finbarr/Downloads/` are the canonical record per v0.6.1 §B implementation note). **Legacy `ApplyV05Rounding(subCriteria []int) int`** helper at `internal/cryptotheses/types.go` now thin-wrapped to `ComputePillarScore` for byte-identical behavior + v0.5.1 #4 application. **`Server` struct extended** with `cryptoWrite *ThesisWriteService` field; initialized after struct literal because depends on `cryptoAdapters` + `cryptoCascade` (already-constructed singletons). **New `internal/server/crypto_theses_write_handlers.go`** with 5 handler funcs + `mapWriteErr` translating service errors to HTTP codes (404 for ErrThesisNotFound, 400 for ErrCannotEditLocked/ErrCannotEditNeedsReview/ErrNotDraft/ErrSelfDependency/ErrDuplicateDependency/ErrNonInfraOracleParent/ErrBadSubCriterion/ErrMissingMandatoryField + adapter-not-found + default unknown). **11 P1 ACs PASSED** via new `cmd/ft-d25-p1-test/` ephemeral SQLite harness (P1.AC1 POST DeFi → Q1=1 tie-break verified; P1.AC2 DePIN lock missing RYR → 400 ErrMissingMandatoryField; P1.AC3 RWA lock missing custody → 400; P1.AC5 invalid Q5 enum → 400; P1.AC6 PUT draft → 200; P1.AC7 PUT locked → 400; P1.AC8 lock creates 2+ upward cascade rows; P1.AC9 lock with oracle parent creates forward row + DB count=1; P1.AC10 VETO forces band=Exit + veto_triggered=true; P1.AC11 AAVE/EIGEN/5+pillar fixtures pass; P1.AC12 PPG cap one-below with `Q8_pillar_zero` in failed gates). P1.AC4 (sub-type validation) deferred — sub-type currently free-text on adapter MDs; Phase 2 surfaces dropdown sourced from adapter schema (architectural Q A from handoff). Production `/var/lib/ft/ft.db` unchanged (12 theses + 18 cascade rows). FT binary rebuilt + deployed `/opt/ft/bin/ft`, service restarted clean, `/healthz` confirms green. **Phase 1 ship-gate moved 8/10 → 10/10** (the 2 deferred validators landed). **Phase 2 (frontend dedicated pages — D25 Decision 6) pending.** `FT_Spec_9l_v0_9_patch.md` (Claude Code as-built, Phase 1) authored separately per v0.6.1 §D strict patch immutability — supersedes nothing; supplements v0.8 + v0.6.1. Standby posture pending Phase 2 sequencing call. |
| 1.22.7 | 2026-05-30 (PM late evening, fourth pass) | **Spec 9l v0.8 patch (Claude Code as-built) — EIGEN v1 ingest + lazy-forward-cascade pattern + multi-zero pillar PPG cap validated.** 12th locked thesis: **EIGEN v1** EigenLayer / EigenCloud, Infrastructure (`restaking` sub-type — first non-oracle Infra), `alt_18` scorecard, Q1=1, Q2=0, Q3=2, Q4=1, Q5=0, Q6=1, Q7=2, Q8=0, Q9=2 = **9/18 raw → Trim band via PPG cap** (Q2/Q5/Q8 triple-zero; per Spec 9l §15 single one-band-below penalty applies, not stacked — verified in DB). Q5 mechanism `governance_with_fee_switch` (ELIP-12 proposal in governance, not yet activated; aspirational mechanism scored sub-criterion 1 max = 1, sub-criterion 2 = 0 per DeFi adapter §3 "fee-switch potential" convention). $0 current project revenue to EIGEN holders (CoinGecko 24h verified); FDV $405M; vs-FDV % = 0.00. Holding horizon Medium (3-12mo catalyst trade pending ELIP-12 activation = binary inflection trigger for Trim → Hold → Accumulate re-rate). BTC-β medium (Infrastructure default per adapter §8). No VETO (unlock cliff ~15% in 90d, below 20% threshold; watchlist only). Liquidity venues: Binance, Gate, Bybit. Ingested via new `cmd/ft-seed-9l-eigen/main.go` with idempotent INSERT...ON CONFLICT UPSERT pattern. **Cascade rows added (2 → 18 total in graph)**: ETH v1 → EIGEN v1 [`protocol_host`, moderate] (sixth `protocol_host` cascade; EIGEN restaking layer 100% ETH/LST-denominated) + BTC v1 → EIGEN v1 [`btc_beta_implicit`, weak] (notification-only). **NO forward `oracle_dependency` rows pre-created at lock** per Infrastructure adapter §3 lazy-forward-cascade doctrine — future LRT theses (EtherFi/Kelp/Renzo/Puffer) or DA-consuming rollup theses will create forward rows when they lock declaring Q6 sub-criterion 4 references to EIGEN. **5 cascade firing acceptance tests run + PASSED via new `cmd/ft-cascade-firing-eigen`** (non-destructive, snapshot/restore + post-test `cascade_events` cleanup): (5.3) LINK 16 Strong → 11 Hold 2-band drop fires AAVE MEDIUM via forward `oracle_dependency` moderate; (5.4) SOL 13 Accumulate → 5 Trim 2-band drop fires RNDR MEDIUM via `protocol_host` moderate; (5.5) BUIDL 17 Strong → 11 Hold 2-band drop fires ZERO children (BUIDL is RWA leaf, no downstream cascade dependencies); (5.6) ETH 13 Strong → 9 Hold 2-band drop fires 5 children ARB HIGH + LINK/AAVE/BUIDL/EIGEN MEDIUM (was 4 children pre-EIGEN; engine handles expanded graph correctly with no special-casing for new child); (5.7) EIGEN simulated Hold → Exit 2-band drop fires ZERO children — **lazy-forward-cascade pattern validated** (EIGEN has zero forward rows in graph despite trigger conditions satisfied). **Infrastructure adapter intra-adapter range now 7 points** (LINK 16 → EIGEN 9) — widest spread in framework, validates adapter quality discrimination across `oracle` (clean, active value capture, multi-cycle moat) vs `restaking` (structural moat, broken current value capture, ELIP-12 catalyst pending) sub-types. **PPG cap pattern catalogue extended to 5 distinct patterns** with EIGEN's new **"value-capture failure pattern"** (real protocol value + ZERO token value capture + chart drag → Trim) — structurally distinct from prior timing-failure patterns (AAVE/SOL/AVAX) and hybrid-failure patterns (ARB/POL). **5th v0.5.1 doctrinal candidate** for 2026-08 quarterly audit: explicit codification of aspirational vs operational mechanism scoring convention (Q5 mechanism strength = operational not aspirational; proposed mechanisms score per fee-switch-potential convention sub-criterion 1 max = 1, sub-criterion 2 = actual value flow not projected; explicit re-score triggers required for proposal-to-activation events). Carries forward 4 v0.5.1 candidates from v0.6 §M (Q1 4-pillar tie-breaking, DePIN Q2sc5 BME, RWA Q8 stable-peg, qualified-purchaser RWA liquidity exception). `_crypto_scoring_log.md` updated: 8 → 12 theses; cascade graph 9 → 18 rows; v0.6/v0.7/v0.8 amendments + EIGEN row; distribution diagram added (3-17 score spread across 8 adapters); calibration anchor reference revised post-v0.6 §sE (BUIDL 17/18 framework ceiling; LINK 16/18 Infrastructure oracle ceiling; EIGEN 9/18 Infrastructure restaking lower-bound). Phase 1 ship-gate unchanged at 8/10. No DB migration required (Q5 enum already extended in 0033; `governance_with_fee_switch` was added then). Claude Code standby posture resumed pending D25 Scoring Engine modal UI + D13-D16 cron framework + D17 Liquidity pre-filter (all Phase 2). `FT_Spec_9l_v0_8_patch.md` (Claude Code as-built) captures full pass. |
| 1.22.6 | 2026-05-30 (PM very late, third pass) | **Spec 9l v0.6 doctrinal + v0.7 as-built — Batch 3a + Migration 0033 land.** Three new locked seed theses ingested via `cmd/ft-seed-9l-batch3a/`: **AAVE v1** DeFi lending 13/18 raw → Accumulate via PPG cap (Q8=0); first DeFi `protocol_host` cascade (ETH→AAVE moderate); **first forward `oracle_dependency` cascade in framework** (LINK→AAVE moderate per Infrastructure adapter §3 bidirectional design); Q5 `real_yield_staking` (Umbrella system + $50M buyback program). **RNDR v1** DePIN compute 14/18 Strong Conviction (lower edge, clean — no PPG cap); first DePIN `protocol_host` cascade (SOL→RNDR moderate); **first mandatory RYR quantification in framework** (RYR=0.35 = CONCERN band per DePIN adapter §3, bootstrap window expired Nov 2025 at 30 months network age); Q5 `burn_and_mint`. **BUIDL v1** RWA treasuries 17/18 Strong Conviction (upper edge, clean — no PPG cap); first RWA `protocol_host` cascade (ETH→BUIDL moderate); **first mandatory RABR + Custody Verification Tier in framework** (RABR=1.00 fully backed, BNY Mellon as G-SIB tier_1 custodian with monthly attestation cadence in US jurisdiction); Q5 `direct_asset_claim`; **highest score in framework** above LINK 16/18. Liquidity pre-filter exception applied per v0.6 §F.4 (qualified-purchaser RWA: alternative venues `securitize_platform/circle_usdc_redemption/bny_mellon_admin`). **Migration 0033 applied on FT startup**: table rebuild for Q5 mechanism CHECK (7→14 values) + BTC β CHECK (4→5 with `reference`) + ALTER ADD of 12 new nullable columns (4 DePIN RYR + 5 RWA RABR + 3 RWA Custody). 8 indexes from 0031 re-created. 2 Speculative horizon triggers from 0032 re-created. Post-migration atomic UPDATE transaction: 5 Q5 mechanism re-tags (LINK→`required_for_service`, AAVE→`real_yield_staking`, BUIDL→`direct_asset_claim`, LUNC→`burn_and_mint`, RNDR→`burn_and_mint`) + BTC β re-tag (`low`→`reference`) + RNDR RYR populate + BUIDL RABR populate + BUIDL Custody Tier populate. **Go type extensions** (`internal/cryptotheses/types.go`): `BetaReference` BTCBeta constant + 7 new Q5Mechanism constants + new `CustodyTier` enum (tier_1/tier_2/tier_3) + Valid() updates. **API extension** (`internal/cryptotheses/thesis_service.go`): `ThesisDetail` gains 12 new optional fields with `omitempty` (DePIN RYR: 4; RWA RABR: 5; RWA Custody: 3) + 3 computed classifications (`ryrClassification`, `rabrClassification`, `bootstrapWindowStatus`) via `classifyRYR`/`classifyRABR`/`classifyBootstrapWindow` helpers. Non-applicable adapters (e.g., ETH on L1) return clean payloads without the DePIN/RWA-specific keys. **Final state**: 11 locked theses across 5 adapters (BTC, L1, L2, Infrastructure, DeFi, DePIN, RWA, Speculative); 16 cascade rows; 4 of 5 cascade types deployed (added `oracle_dependency`; only `narrative_correlated` unused by design — manual creation only); Phase 1 `other`+free-text Q5 workaround retired. Cascade firing test post-Batch-3a: ETH 13/18→9/18 (2-band drop) fires 4 children (ARB HIGH platform_parent + LINK/AAVE/BUIDL MEDIUM protocol_host) — engine handles expanded graph correctly. **10 acceptance criteria from Migration_0033_Doctrine_Handoff.md**: 8/10 functional (cascade engine + API surface + omitempty + RNDR RYR computed `concern` + BUIDL RABR computed `fully_backed` + LINK/BTC re-tags + 16-deps preserved) + 2 deferred to D25 build (write-side validators on DePIN/RWA mandatory fields). **v0.6 patch (Claude.ai forward-looking doctrinal)** + **v0.7 patch (Claude Code as-built execution)** form the canonical record. Four v0.5.1 doctrinal clarification candidates queued for next quarterly methodology audit (2026-08): Q1 4-pillar tie-breaking, DePIN Q2 sub-criterion 5 BME interpretation, RWA Q8 regime sub-criteria weighting for stable-peg, qualified-purchaser RWA liquidity pre-filter exception. Framework ceiling question resolved (v0.6 §E): pure scoring math wins — BUIDL at 17/18 above LINK at 16/18 is honest framework behavior; strategic positioning captured by horizon + BTC-β tags, not artificial caps. Phase 1 ship-gate unchanged at 8/10. Claude Code standby posture resumed. |
| 1.22.5 | 2026-05-30 (PM late, second pass) | **Spec 9l v0.5 patch §L.9 — two position reversals locked.** Claude.ai's second-pass response (`Response_to_Claude_Code_v0_5_Patch.md`) reversed §L.3 and §L.4 based on Claude Code's §F empirical validation in the original v0.5 patch. **§L.9.1 v0.5 rounding rule LOCKED canonical** (supersedes v0.4 §C): *"Round down if any sub-criterion = 0, OR if 2+ sub-criteria ≤ 1 in a pillar with 5+ sub-criteria. Otherwise round to nearest."* Reversal reasoning (Claude.ai): §F validation table showed the rule preserves all v0.4 outcomes for the original calibration triad (BTC/ETH/LUNC/ARB), captures batch-2 manual overrides (LINK Q2, SOL Q9) as the doctrinal default, and has zero false-positive matches. **§L.9.2 4-sub-criterion pillar interpretation: Option B (strict 5+ reading)** — narrowing to 4 would be a v0.5.1 patch, not a §L sub-decision. **§L.9.3 LINK v1 re-locked at 16/18** (was 14/18 at initial lock): Q6 (1.83 avg, 1 sub-crit at 1 in 6-pillar) → nearest 2; Q9 (1.83 avg, same calc) → 2. Total 14 → 16, band unchanged Strong Conviction. Reversal of original §L.4 ceiling-integrity reasoning: Infrastructure adapter §11 targets ~14/18 as lower edge (not ceiling); framework doctrine permits Infrastructure to match/exceed L1 due to structural Q5 advantage; ETH 13/18 reflects current macro not framework hard ceiling. **§L.9.4 DB execution complete**: atomic transaction with pre-state + post-state rows in `crypto_thesis_history` (event_type=event_rescore, action=`doctrine_methodology_lock` + `doctrine_methodology_lock_applied`); pillar_scores_json mutated via `json_set` for Q6 + Q9; total_score 14 → 16; API verified at `GET /api/crypto/theses/LINK/v1` returns 16/18 Strong Conviction with Q6=2 + Q9=2. No fork (pillar identities unchanged; methodology cleanup, not substantive thesis content change). **§L.9.5 Re-verification of other 7 locked theses** under v0.5: BTC (/12 doesn't apply), ETH (Q2 1.8 with 1 sub-crit at 1 → already 2, no change), ARB (Q2 has zeros → already 0), LUNC (Q2 has zero → already 1), SOL (Q2/Q6/Q9 all 2+ sub-crits at 1 → already 1), POL (all-1 sub-crits → already 1), AVAX (Q4/Q9 1 sub-crit at 1 → already 2). Only LINK needed re-locking. **§L.9.6 Process reflection** (Claude.ai): "Empirical validation from Claude Code against locked anchors > Claude.ai's original heuristic." Pattern recorded for future cascade. **Code change**: `ApplyV05Rounding([]int) int` doctrine helper added at `internal/cryptotheses/types.go` (clamp [0,2], rule docstring inline); ready for D25 Phase 2 implementation when sub-criterion → pillar math runs. **`_crypto_scoring_log.md` updated**: LINK row score → 16/18; Section 5 amendment row added documenting v0.5 lock + LINK re-lock. **`Response_to_Claude_Code_v0_5_Patch.md` (Claude.ai) + `FT_Spec_9l_Crypto_Thesis_Framework_v0_5_patch.md` (Claude Code, updated §L.9)** form the canonical record. Phase 1 ship gate unchanged at 8/10. Claude Code standby posture resumed; Batch 3a authoring begins on Claude.ai side under v0.5 canonical rule. |
| 1.22.4 | 2026-05-30 (PM late) | **Spec 9l v0.5 patch §L resolutions locked.** Claude.ai response (`Response_to_Claude_Code_Post_Batch_2.md`) closes all 5 decision questions raised in the post-batch-2 handover. v0.5 patch §B + §F status changed from OPEN → RESOLVED. **§L.1 Batch 3a authoring approved** (AAVE→RNDR→BUIDL; ~1 work session each on Claude.ai side; NOT in user's portfolio but acceptable for framework validation). **§L.2 0033 timing: AFTER Batch 3a** (Phase 1 `other` + free-text fallback handles ingestion; real RYR/RABR data shapes inform proper column design; re-tag cost trivial). **§L.3 v0.5 rounding rule: DEFER to quarterly methodology audit** — v0.4 *"round down if any sub-criterion = 0; else round to nearest"* stands canonical. Honest reasoning: Claude.ai's inconsistent prose-math application surfaced the "gap"; not evidence of framework limitation. Quarterly audit (next 2026-08) decides on v0.5 refinement after 5-10 more theses score under strict v0.4. **§L.4 LINK v1: honour 14/18; amend MD prose in Batch 3d** — current DB state correct (Q6=1, Q9=1 stored). 5 reasons recorded: calibration intent of ~14/18 ceiling for Infrastructure, Q6/Q9 sub-criterion 5 = 1 are real drags, ETH ceiling at 13 means LINK at 16 would break framework. **§L.5 D25 Scoring Engine sequenced ahead of Spec 9m** by wide margin. **§L.6 other ambiguities resolved inline**: ETH v1 Q2 prose amendment = Batch 3d hygiene (no urgency, DB matches v0.4 §C); BTC `btc_beta` enum `reference` value rolls into 0033 (5-way enum, re-tag BTC v1 from `low` → `reference` in same migration); LUNC mechanism re-tag to `burn_and_mint` at 0033; `_crypto_scoring_log.md` stays at `/var/lib/ft/research/crypto/`; Spec 9m defers (trade-history pipe gating). **§L.7 canonical sequencing locked**: 3a author → 3a ingest → 0033 → 3d prose amendments → D25 → Phase 2 crons → Spec 9m future. **§L.8 Claude Code standby posture**: no proactive build work until Batch 3a delivers; flag only if ft.service degrades or cascade engine misfires. |
| 1.22.3 | 2026-05-30 (PM) | **Spec 9l batch 2 ingest + v0.5 status-capture patch.** Four new locked v1 seed theses ingested via `cmd/ft-seed-9l-batch2/`: LINK Infrastructure oracle (14/18 Strong Conviction lower edge — first non-L1 Strong; first `protocol_host` cascade ETH→LINK moderate; Q5 mechanism stored as `other` with canonical label `required_for_service` in note per v0.4 §B Phase 1 rule), SOL L1 hp-l1 (13/18 raw → Accumulate via PPG cap Q8=0; Alpenglow Q3 2026 catalyst; Drift exploit MONITOR), POL L1 est-alt-l1 primary + L2 zk secondary advisory (8/18 raw → Trim via PPG cap; **first hybrid coin under §13**; primary/secondary delta = 4 points at flag threshold), AVAX L1 est-alt-l1 (11/18 raw → Hold via PPG cap; anti-narrative-state test case for Avalanche9000 renewal). 5 new cascade rows: 1 protocol_host (ETH→LINK moderate) + 4 btc_beta_implicit (BTC→[LINK/SOL/POL/AVAX] weak) — total 9 cascade rows now. Cascade firing acceptance tests extended (`cmd/ft-cascade-firing/`): ETH 13/18→11/18 single band drop fires HIGH on ARB only (platform_parent strong); LINK does NOT fire (protocol_host moderate needs 2+ bands); ETH 13/18→9/18 2-band drop fires both ARB (HIGH) + LINK (MEDIUM) with LINK status auto-updating to needs-review. Non-destructive test cleanup confirmed. API extension: `ThesisDetail.SecondaryAdapterSlug` + `SecondaryAdapterType` exposed via GET endpoint for hybrid-coin frontend display ("+ l2 advisory" tag on POL detail modal). `_crypto_scoring_log.md` initialised per v0.2 §"Item 4" 16-column schema with 8 entries + VETO event log + methodology amendment log + cascade graph state + quarterly audit checklist (staged at `/var/lib/ft/research/crypto/`). v0.5 patch (status-capture only — no doctrine locked) flags two product-owner items: (a) LINK v1 header-vs-prose math inconsistency (header 14/18 vs prose-strict-v0.4-rounding 16/18; same pattern as ETH v1 Q2; Claude Code applied same trust-the-locked-header resolution with Q6=1 + Q9=1 in DB), (b) v0.5 rounding rule refinement candidate surfaced by batch 2 prose ("round down if any sub-criterion = 0 OR if 2+ sub-criteria ≤ 1 in 5+-pillar; else nearest"); proposed rule preserves all v0.4 outcomes for calibration triad + ARB AND captures batch 2 author's manual overrides as default; recommendation: lock now to unblock Batch 3a (DeFi/DePIN/RWA seed theses). Calibration gap remaining: DeFi/DePIN/RWA adapters locked but zero theses authored. Phase 2 migration 0033 scope consolidated: 7 new Q5 enum values + DePIN RYR fields + RWA RABR + Custody Verification Tier + optional BTC `reference` btc_beta value. Cascade types deployed: 3 of 5 (`platform_parent`, `protocol_host`, `btc_beta_implicit`); `oracle_dependency` waits for downstream DeFi/Speculative thesis referencing Infrastructure; `narrative_correlated` is manual-creation only by design. Phase 1 acceptance gate unchanged at 8/10 (D25 Scoring Engine modal + cron framework deferred to Phase 2). Commit `a476640`. Handover authored at `/home/finbarr/Downloads/Handover_for_Claude_ai_Spec_9l_Post_Batch_2.md` for next Claude.ai session pick-up. |
| 1.22.2 | 2026-05-30 | **Spec 9l v0.4 patch — Phase 1 ship gate.** All 5 remaining adapter MDs locked (l2/defi/infra/depin/rwa at v1) via extended `cmd/ft-seed-9l` seeder. ARB v1 calibration thesis seeded (8/18 raw → Trim band via PPG cap; parent ETH v1 via `platform_parent` strong cascade — first formal cascade row in framework; pillar_pass_gate_failed=1 from Q2=0+Q5=0+Q8=0). All 4 calibration anchors now in place. **Step 5 cascade engine** (`internal/cryptotheses/cascade.go`): async single-pass walker, BFS depth-5 recursion detection at dep-creation, evaluateTrigger() per (dep_type × strength × band drop), cascade_events audit log; 6/6 acceptance criteria verified via `cmd/ft-cascade-test` against prod DB (ETH 13→11 demotion fires HIGH on ARB, reverse cascade does not fire, circular dep rejected, btc_beta_implicit notification-only on BTC→Trim). **Step 7 cross-thesis table + per-thesis detail** (`internal/cryptotheses/thesis_service.go` + `internal/server/crypto_theses_handlers.go`): 16-column ListAll with parent-symbol cascade join; Get with pillar scores + Q5 detail + cascade deps both directions + history; new endpoints `GET /api/crypto/theses`, `GET /api/crypto/theses/{symbol}/{version}`, `GET /api/crypto/theses/{symbol}/{version}/events`. **Step 8 Allocation Panel UI**: inline panel at top of Crypto Theses tab with 5 number inputs + live sum-to-100 validation + computed alt allocation suggestion table; `GET/PUT /api/crypto/allocation` with two-table (current+history) backing. **Step 8 last — 9e sell-window verdict badge** (D30): frontend-only addition to Crypto Indicators tab; fires when 9e composite is Distribute/Strong-Distribute AND any locked Crypto Thesis is in Trim/Exit band; amber/red gradient banner above hero with clickable thesis links opening detail modal. Frontend: `renderCryptoTheses` restructured into Allocation Panel → Cross-thesis table → Adapter Repository three-section layout; new `openCryptoThesisDetail` modal with pillar table + Q5 mechanism + Q9 note + cascade deps + VETO banner + collapsible MD body. **Doctrine locked in v0.4 patch**: (1) Two-table allocation schema supersedes v0.2 §"Item 7" `user_settings` extension; (2) Q5 mechanism enum extension plan for Phase 2 (7 new values including direct_asset_claim/required_for_service/dsr_surplus/burn_and_mint/buyback_stake/real_yield_staking/governance_with_fee_switch); (3) New rounding rule "round down if any sub-criterion = 0; else round to nearest" preserves PPG protection + ETH Q2=2 reality; (4) ETH v1 Q2 reconciliation at 13/18 with new rule (no DB update needed — seeded values already match). **Phase 1 ship-gate status: 8/10 kickoff acceptance criteria green**; Scoring Engine modal UI (D25) + cron framework (D13-D16) deferred to Phase 2 with clean architecture stubs in place. |

---

*Personal use only. Not investment advice.*
